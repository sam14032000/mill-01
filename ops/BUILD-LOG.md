# Build Log

One line per completed part of `docs/build-guide.md`. What was done, and anything the founder had to run by hand.

- **Part 4 — Tailscale.** Founder ran manually: `tailscale up --ssh` on the box and installed Tailscale on their phone, same account. Verified via `tailscale status` — `ubuntu-4gb-nbg1-2` (this box) and `nothing-phone-3a` both present on the tailnet. Founder then removed the Hetzner Cloud Firewall's inbound port 22 rule, so SSH is now reachable only over Tailscale.
- **Part 5 — Docker.** Installed Docker CE via `get.docker.com`; added `agent` to the `docker` group (takes effect on new SSH sessions); wrote `/etc/docker/daemon.json` (log rotation, ulimits) and restarted the daemon. `docker run --rm hello-world` succeeded both before and after the daemon restart.
- **Part 6 — Pi.** Verified `@earendil-works/pi-coding-agent` exists on npm (matches `github.com/earendil-works/pi`, MIT licensed) before installing, per instruction not to guess the package name. Installed globally with `npm install -g --ignore-scripts @earendil-works/pi-coding-agent`. `pi --version` → `0.84.3`, recorded in `README.md`. Not yet configured — installed as orchestrator only, per D-06 working prototypes never run in this context.
- **Part 7 — LiteLLM.** Wrote `~/stack/litellm/docker-compose.yml`, `config.yaml` (flash / audit / mechanical model routes), and an empty `.env` template. Founder populated `.env` over SSH by hand — no secret ever passed through this session's context. `docker compose up -d` brought up postgres, redis and litellm; `/health/readiness` returned healthy. Generated the three virtual keys (`mill-flash` $60/30d, `mill-audit` $35/30d rpm 5, `mill-mech` $10/30d) via the LiteLLM master key and wrote them to `~/.config/mill/env` (`chmod 600`) only — not committed, not echoed to the repo. `MINIMAX_API_KEY` is still blank in `.env`, so the `mechanical` route won't work until the founder fills it in. `jq` installed (needed for parsing key-generation responses and later by `healthcheck.sh`). Memory headroom checked post-stack: ~2.1GB available of 3.7GB, swap barely touched.
- **Part 7.3 — second pass, required.** Founder caught that `~/.config/mill/env` held the four variable names with empty values (86 bytes, `MILL_FLASH_KEY` length 0 after sourcing) — the keys were never actually written. Root cause: `jq` was not yet installed at the moment the first `/key/generate` responses were piped through `jq -r '.key' > /tmp/*.key`, so those extraction commands silently failed and wrote empty files; `jq` was installed immediately afterward but only used to re-parse the JSON for the confirmation message, not to redo the extraction, so the env file was written from empty variables. Exit codes on the write step were all 0, which is why it went unnoticed at the time — a reminder not to report success from an exit code alone. Fixed by deleting all three key aliases (`/key/delete` with `key_aliases`), regenerating them fresh, sanity-checking the extracted key length *before* writing the env file this time, then verifying with `source ~/.config/mill/env && echo ${#MILL_FLASH_KEY}` (25, non-zero for all three) and a live test completion through the proxy (`POST /chat/completions` with `mill-flash` against `flash` → HTTP 200, model replied "ok").
- **Part 8 — Repo.** Created `minds/{amit,priya,rohan}/{captures/,profile.md,graveyard.md}` and `minds/shared/{themes.md,dynamics.md}`; removed two stray files (`minds/shared/profile.md`, `graveyard.md`) that the guide's literal `minds/*/` glob would have created but that don't match the documented layout. Generated `~/.ssh/github_deploy` and wired it into `~/.ssh/config`. Founder added the deploy key to GitHub with write access; switched `origin` to `git@github.com:sam14032000/mill-01.git` and verified with a real push (`b0a4087..9066e7c`), not a throwaway test commit. Fixed a `.gitignore` typo (`__pychache__/` → `__pycache__/`). Founder caught that the build guide says `~/workspace/mill` and `github.com:YOU/mill` throughout Parts 3, 9, 11, 12 (systemd `WorkingDirectory`, pi-chat config path, `ops/research.py`'s `REPO` constant, healthcheck/cron paths, the clone command itself), but the actual repo is `~/workspace/mill-01` / `sam14032000/mill-01` — corrected all nine references in `docs/build-guide.md` and pushed (`9066e7c..abeb7d2`).
- **Part 8b — founder renaming, D-31 superseded.** Founder directories renamed `minds/{amit,priya,rohan}` → `minds/{saksham,amisha,vaibhav}` to match the real founders; same fix applied to the placeholder names in `docs/runbook.md` and `docs/build-guide.md`. `docs/DECISIONS.md`: D-31 (raw captures private by default) marked SUPERSEDED — all three founders explicitly agreed to open raw captures via the shared repo; added D-38 recording that captures still arrive privately by DM but are readable by all three once committed and pushed. `CLAUDE.md` updated to match. Pushed (`75a505f..c7f3732`).
- **Part 9 blocked, then unblocked — the `pi-chat` premise was wrong.** Before writing anything, checked whether `@earendil-works/pi-chat` exists on npm as Part 9.2 assumed — it doesn't, under any Pi-maintainer scope. The real `earendil-works/pi-chat` GitHub repo bridges **Discord and Telegram only**, never Slack — directly contradicting D-03/D-05's stated rationale for choosing Pi and Slack. Stopped and reported this rather than installing an unvetted similarly-named package. Founder confirmed the Slack claim originated in Pi's own upstream README (propagated, not invented here) and had two real reference bridges checked: `comsysto/pi-slack-bridge` and `tintinweb/pi-messenger-bridge`, read for their Slack block-splitting (conservative ~6000-char limit, markdown-boundary-aware splitting) and message-length patterns, but explicitly not adopted — both use a single-trusted-user challenge-code claim model built for one person's own Pi terminal session, wrong shape for three founders needing simultaneous trust. `docs/DECISIONS.md`: corrected D-03 and D-05 in place (marked, not deleted), added D-39 (why a custom Bolt bot, not a pi-chat bridge) and D-40 (the actual Slack auth model: verified `user_id` against a static allowlist, silent drop for anyone off it, no passphrase). Pushed (`c7f3732..cb0121f`).
- **Part 9 — application spec arrived, build-guide split.** Founder uploaded `docs/COMMAND.md` (renamed to `docs/COMMANDS.md` for consistency with what was referenced) — the slash-command spec that no package, `pi-chat` or otherwise, actually supplies. Added to `CLAUDE.md` imports. Added `docs/DECISIONS.md` D-41 recording that the command layer was unspecified since v1.0 as a direct consequence of D-03's incorrect `pi-chat` premise. Split `docs/build-guide.md` Part 9 into transport-only (this pass) and a new Part 9b for command implementation, in COMMANDS.md's specified build order (`/attack` first, `/audit` last). Pushed (`e93cd2b..93be4db`).
- **Part 9 — transport implementation.** Wrote `ops/slack-bot/` (Slack Bolt SDK, Socket Mode): `allowlist.js` (D-40's static map), `capture.js` (writes DM text to `minds/<founder>/captures/YYYY-MM-DD.md` in the spec'd `- HH:MM — text` format), `git-batch.js` (commits+pushes captures every 15 minutes, never blocks on failure), `index.js` (wires the DM→capture path and stubs all eight slash commands — ephemeral "not implemented, see Part 9b" for allowlisted senders, silent empty ack for anyone else). Smoke-tested without live Slack credentials: capture format/location verified directly, allowlist resolution verified for known/unknown/unset IDs, Bolt's `App()` construction verified against a real (rejected) `auth.test` call, and the full write-capture → batch-commit → push path verified end-to-end against the real repo. Pushed (`348da72`, `2c62a43`). **Still waiting on the founder** to finish Part 9.1 in the Slack UI (Socket Mode, scopes, slash commands, install) and hand over the bot/app tokens plus the three founders' Slack user IDs before the acceptance test (a real DM producing an attributed capture; an off-allowlist DM producing nothing) can run.
- **Part 9 — reloadable config, #mill-ideas rename.** Founder confirmed credentials were in `~/.config/mill/env` (read variable names only, per instruction — never the values). `allowlist.js` replaced by `config.js`: reads the env file directly from disk rather than `process.env`, so `SIGHUP` (or a plain `systemctl restart`) picks up a newly-added founder or changed channel id without a code change; blank `FOUNDER_*` values are filtered out (not every founder is onboarded yet) and every remaining value is asserted to start with `"U"` before being trusted; active/inactive founders and configured/missing channels are logged at startup and on every reload. Channel ids now come from `SLACK_CHANNEL_MILL/RESEARCH/GRAVEYARD` env vars, never hardcoded. Smoke-tested the reload path directly against a scratch env file (add-founder-then-reload takes effect only after `load()` runs; a non-`U`-prefixed value is rejected and logged). While testing against the real env file, found `FOUNDER_AMISHA` and `FOUNDER_VAIBHAV` were **not** blank as expected — both held 4-character values starting with `U`, well short of a real Slack ID (9–11 chars). Flagged rather than silently trusting them; founder is fixing the env file directly, full end-to-end verification still pending. `#mill` renamed to `#mill-ideas` throughout `docs/COMMANDS.md` and `docs/runbook.md` as asked (workspace collision with the bot's own name), and the same fix extended to `CLAUDE.md` and `docs/DECISIONS.md`'s matching tables for consistency. Pushed (`e4207c5`).
- **Part 9 — complete.** Founder fixed the env file (`FOUNDER_SAKSHAM` 11 chars, a real Slack ID; `FOUNDER_AMISHA`/`FOUNDER_VAIBHAV` genuinely blank now). Installed and started `mill-chat.service` per Part 9.3; logs confirmed `config: active founders: saksham` and `Slack bot connected (Socket Mode)` on first boot. Log rotation config's dry run (`logrotate -d`) caught a real gap — it refused to rotate because systemd creates the redirected log file as root before dropping to `User=agent`, leaving `~/logs` looking insecure to logrotate; fixed by adding `su agent agent` to `/etc/logrotate.d/mill`, re-verified clean. **Acceptance test passed live:** a DM from the founder's real Slack account produced a correctly attributed capture (`minds/saksham/captures/2026-08-26.md`) and the ✅ reaction, confirmed by the founder directly in Slack. The off-allowlist silent-drop path was not live-tested (founder has no second Slack account) — confidence in it rests on the code path and the earlier unit tests (D-40's `founderForUserId` returning `null` for unknown/unset IDs), not a live confirmation; noting that gap rather than claiming full verification. Service is both `enabled` and `active`, so it survives a reboot. Holding here per the founder's instruction: Part 9b does not start until Part 9 is confirmed working, which it now is.
- **Part 9b — `/attack` implemented.** Built against `docs/COMMANDS.md`: `ideas.js` (4-char hex id generation with collision check, `idea.md`/`state.json` writers), `llm.js` (LiteLLM `flash` call wrapper), `telemetry.js` (schema is a working assumption — `docs/EVAL.md` doesn't exist in the repo yet despite `docs/COMMANDS.md` specifying telemetry "per the EVAL.md schema"; kept flat and additive to reconcile later), `git.js` (shared commit-and-push, refactored out of the captures-only `git-batch.js` — ideas commit immediately on creation rather than batching, since each is a discrete low-frequency event), and `commands/attack.js` (the handler: D-40 allowlist check, retry-once-on-missing-`ASSUMPTION`, posts to `#mill-ideas`). All unit-tested directly (assumption parsing edge cases, idea file writes, telemetry format) before any live model call.
- **Part 9b — false trail on `/attack`'s first live test, then resolved.** The first real end-to-end call hung indefinitely. Chased it through three layers of plausible explanations, each individually disproven before moving to the next: (1) suspected a hung client/stuck LiteLLM worker — ruled out by testing DNS, TCP+TLS, and Redis connectivity from inside the LiteLLM container directly (all sub-100ms) and by restarting the container fresh; (2) suspected `thinking_level` wasn't reaching Gemini — confirmed via `LITELLM_LOG=DEBUG` tracing that LiteLLM's Gemini request-builder actually was dropping `thinking_level` from the outbound `generationConfig` regardless of `drop_params`, a real bug, reported to the founder with the exact debug-log evidence; (3) attempted a Gemini pass-through route (`/gemini/v1beta/models/...:generateContent` with `x-goog-api-key` auth) to bypass LiteLLM's mapping entirely — still slow, and a controlled low-vs-medium comparison came back with identical `thoughtsTokenCount` (111 both), which should have been the signal to stop looking at LiteLLM. **Founder identified the actual root cause: the required Google Cloud API wasn't enabled on the project.** A disabled API surfaced as latency (every call eventually returned 200, just after 60-180s+) rather than a clean error, which is what made it look like a behavioral bug worth chasing through the stack instead of an access problem to check outside it. Confirmed post-fix: a trivial prompt on `flash-fast` now completes in ~3-4s.
- **Part 9b — `/attack` complete and verified.** Kept the `flash-fast`/`flash` split and `thinking_level` config from the false-trail work — still correct even though it wasn't the actual cause; `docs/DECISIONS.md` D-08 amended (interactive commands run `thinking_level: low`, research runs `medium`, never unspecified; the false trail recorded there too, since "leave the stack when every layer inside it reports healthy" is the more durable lesson). `llm.js` and `commands/attack.js` updated to call `flash-fast` with `max_tokens: 4096` and to surface `usage.completion_tokens_details.reasoning_tokens` through to telemetry. Real end-to-end test via the actual production code path (`runAttack`, not raw `curl`): **15s wall-clock**, `reasoning_tokens: 676`, `text_tokens: 617`, correctly parsed into a genuinely falsifiable assumption. The `mill-flash` virtual key was updated via `/key/update` (never regenerated — the deployed key value in `~/.config/mill/env` was preserved throughout) to allow `flash-fast`, briefly also `gemini-3.7-flash` for the pass-through diagnosis, then reverted back to just `["flash","flash-fast"]` once pass-through was no longer needed. One mistake worth noting: an early `/key/update` response was piped to `jq` without filtering, which printed the live key value in plaintext into this session's tool output — caught immediately, no further raw `/key/*` responses were printed unfiltered afterward.
- **Part 9b — thinking-level measurement (24 runs total) and `/attack` prompt hardening.** Founder asked for a measured comparison before settling on `thinking_level` rather than trusting the earlier single false-trail data point. All runs used the Gemini pass-through route (`/gemini/v1beta/models/...:generateContent` with `x-goog-api-key` auth), the verbatim `/attack` system prompt, `maxOutputTokens: 4096`. The `mill-flash` key's `models` list was temporarily widened to include the raw `gemini-3.7-flash` name for pass-through access, then reverted to `["flash","flash-fast"]` after each measurement pass.

  **First pass — one idea, three levels** (batter subscription idea): low 6.42s / 333 thinking / 646 visible / $0.003771; medium 7.34s / 678 / 493 / $0.004491; high 14.70s / 1204 / 670 / $0.007127. At 150 exchanges/month all three levels land under 2% of the $60 Gemini budget line — the reasoning-token cost is real in proportion but small in absolute dollars for this command; latency (not cost) is the visible difference between levels.

  **Second pass — 15 runs, Part A (3 ideas × 3 levels) + Part B (variance check, batter idea × low×3, medium×3).** Full table and all verbatim assumption lines are in the session transcript; the operationally important finding is the variance check: three `low` runs on *identical* input produced `thoughtsTokenCount: 0` all three times (versus 333 in the first pass on the same idea) while three `medium` runs stayed consistently non-zero (856–1171). `low` is a ceiling the model may not use at all, not a fixed reasoning budget — and wall-clock doesn't track thinking-token count cleanly either (one zero-thinking `low` run took 14.75s, longer than several `medium` runs that did think). One script bug along the way: a manifest file built without a trailing newline silently dropped its last entry from a `while read` loop (a classic bash gotcha) — caught by verifying run count against expected count before reporting, re-ran the missing case manually.

  Also surfaced by Part A: the deliberately-weak idea ("an AI app that helps people be more productive") got silently reframed into a plausible-sounding falsifiable assumption at **all three thinking levels**, none refused or flagged the vagueness. This is what motivated the `/attack` prompt changes below — an unfalsifiable idea sliding through `/attack` would otherwise consume a real research pass and audit before anyone caught it.

  **Decision applied** (founder's call, not mine to make): `flash-fast` stays at `thinking_level: low` for `/attack`, `/think`, `/cross`, `/blindspot`, `/themes`, `/proto`. `flash` (research) stays at `medium`, untouched — its inputs are orders of magnitude larger and untested at any level, so no change was made there. Audit gate untouched (Fable 5, not Gemini). `docs/COMMANDS.md`'s `Model:` lines updated for the five other `flash-fast` commands to stay accurate (metadata only, not prompt text, since the founder said not to edit the other command prompts).

  **`/attack`'s system prompt hardened**, two additions per `docs/COMMANDS.md`: (1) the assumption must now contain a number (price/percentage/count) and name the specific alternative being displaced — low's SaaS run in the first pass lacked a price while medium/high both added one unprompted, so this makes it mandatory rather than emergent; (2) a `TOO_VAGUE:` refusal path — if the idea doesn't name a specific customer, mechanism, or context, the model must say so and name what's missing rather than inventing specifics, and no idea gets created. `commands/attack.js`'s parser (`extractAssumption` → `parseAttackResponse`) now returns a discriminated `{kind: "assumption"|"too_vague"}` result; `handleAttackCommand` posts the `TOO_VAGUE:` line as-is with no retry and no idea write when refused.

  **Verified live against the real production code path** (not raw `curl`), both at `low`: the same weak idea now returns `TOO_VAGUE: the specific target user (e.g., freelance designers, enterprise sales reps), the exact productivity mechanism (e.g., automated meeting scheduling, AI email triage), and the existing tool or workflow it is meant to replace.` — refuses as intended, no idea created. The GST/SaaS idea now returns `ASSUMPTION: At least 20% of CA firms managing 50+ clients are willing to pay at least ₹15,000 per year for a dedicated ITC mismatch tool over the built-in reconciliation features already included in their Clear (ClearTax) or Computax subscriptions.` — number and named alternative both present, as required. Service restarted and deployed with the updated code.
- **Part 9b — telemetry per `docs/EVAL.md` Layer 2.** Founder uploaded `docs/EVAL.md` (not added to `CLAUDE.md`'s imports, deliberately, per the note already in that file). `ops/slack-bot/pricing.js` added (Gemini 3.7 Flash cost math, D-08's $0.75/$3.75 rates, revisit-date comment carried over). `telemetry.js` rewritten to match the exact Layer 2 field set (`ts, founder, stage, idea_id, model, tokens_in, tokens_out, cache_hit_ratio, cost_usd, wall_clock_s, verdict, evidence_basis, reason_code`), superseding the earlier "working assumption" schema written before `EVAL.md` existed. Timestamps switched from UTC to IST (`+05:30`, hardcoded — India has no DST) to match the schema's own example exactly; verified the conversion against that example's literal value. `commands/attack.js` now accumulates `tokens_in`/`tokens_out`/`wall_clock_s` across both attempts when a retry happens, so cost reflects everything actually spent, not just the last call. `cache_hit_ratio` is hardcoded to `0.0` with a comment explaining why: LiteLLM's response for this route exposes no cached-token field to compute a real ratio from (checked directly), so this is an honest placeholder, not a measurement. `status`/`reason_code` satisfy `docs/COMMANDS.md`'s own telemetry section (which predates `EVAL.md` and uses `status`/`reason`) alongside `EVAL.md`'s `reason_code` naming — both specs' requirements are met on the same object.

  **Verified live through the real `handleAttackCommand` handler** (mocked Slack `ack`/`client.chat.postMessage` so nothing posted to the live channel unintentionally, but idea creation and git commit ran for real): a successful GST/SaaS `/attack` produced idea `be67` (committed as `196a1e7`) and this telemetry line —
  `{"ts":"2026-08-26T22:37:59+05:30","founder":"saksham","stage":"attack","idea_id":"be67","model":"flash-fast","tokens_in":229,"tokens_out":916,"cache_hit_ratio":0,"cost_usd":0.00360675,"wall_clock_s":8.497,"verdict":null,"evidence_basis":null,"reason_code":null,"status":"ok"}`
  — and the weak idea produced a `TOO_VAGUE` refusal with —
  `{"ts":"2026-08-26T22:38:02+05:30","founder":"saksham","stage":"attack","idea_id":null,"model":"flash-fast","tokens_in":208,"tokens_out":271,"cache_hit_ratio":0,"cost_usd":0.00117225,"wall_clock_s":2.758,"verdict":null,"evidence_basis":null,"reason_code":"too_vague","status":"refused"}`

  **Conformance checks (`docs/EVAL.md` Layer 1, C-01 through C-22) flagged as stale or unimplementable, not fixed** — the founder asked for a list, not a fix, since these predate the Pi/Slack/`COMMANDS.md` changes:
  - **C-14** (`No founder's raw captures readable outside their own DM channel (D-31)`) is now **directly wrong**, not just stale. D-31 is superseded by D-38 — captures are *deliberately* readable by all three founders via the shared repo once committed. Implemented literally, C-14 would fail by design against the system's actual, correct, agreed-on behavior.
  - **C-20** (`No capture file has been modified after its commit (D-25: captures are raw)`) cites the wrong decision. D-25 is "Three founders, $100/month total" — unrelated to captures. The actual raw-captures-never-edited rule lives in `runbook.md` Stage 1 and `CLAUDE.md`'s Conventions section, neither of which carries an explicit D-number to cite instead.
  - **C-05** (`Per-key daily budgets configured and non-null in LiteLLM`) doesn't match what's built. All three virtual keys use `budget_duration: "30d"`, not daily — per `build-guide.md` Part 7.3's own JSON payloads, even though that same part's prose claims "these reset daily." The guide is internally inconsistent; C-05 as literally written checks for something that was never configured.
  - **C-15 through C-18** (Docker sandbox isolation checks) aren't stale but are currently **unimplementable**: Part 10 (the sandbox) hasn't been built yet in this build sequence (still at Part 9b).
  - **C-17** specifically (`Container egress allowlist is present and non-empty`) has a deeper problem beyond "not built yet": D-06 states network egress should be allowlisted, but `build-guide.md`'s actual Part 10 `run.sh` script has no `--network` restriction or egress-allowlist mechanism at all. As currently specified, building Part 10 exactly per the guide would not satisfy C-17 even once it exists.
  - **C-04** (`no direct provider base_url in any config`) is ambiguous rather than clearly stale: `~/stack/litellm/config.yaml`'s `mechanical` entry has `api_base: https://api.minimax.io/v1`, which is LiteLLM's own necessary upstream routing, not application code bypassing the proxy. Worth scoping explicitly (app code vs. LiteLLM's internal config) before implementing, or it will false-positive on day one.
- **Part 9b — real defects from the C-05/C-17 findings fixed; doc citations corrected.** Founder confirmed C-05 and C-17 were real bugs (spec vs. implementation contradicting each other), not just stale references, and asked for fixes rather than just a list this time. Live keys updated via `/key/update` (values preserved, never regenerated): `mill-flash` $2.50/`1d`, `mill-audit` $2.00/`1d`, `mill-mech` $0.50/`1d` — verified via `/key/info` after the change. `build-guide.md` Part 7.3's JSON fixed to match its own "these reset daily" prose, with the daily amounts noted as D-23's monthly caps ÷~30 with headroom. Part 10's `run.sh` spec corrected for C-17 (not built yet, spec only): `--network none` by default, opt-in named `egress` network via `MILL_SANDBOX_NETWORK=egress`, documented honestly as not yet a real per-host allowlist. Found and fixed two more leftover `#mill` references (Part 0.5's channel list, Part 13's checklist) missed in the earlier rename pass while already in the file.

  Documentation-only fixes: C-04 in `docs/EVAL.md` scoped explicitly to application code, excluding LiteLLM's own `config.yaml` where provider `api_base` is expected. C-14 rewritten to check the actual D-38 behavior (captures ARE in the shared repo, attributed) with the old D-31-citing wording marked superseded, not deleted. C-20's mis-citation (D-25, the budget decision) fixed by adding `docs/DECISIONS.md` D-42 — the raw-captures-never-edited rule finally gets a D-number, having previously existed only in `runbook.md`/`CLAUDE.md` prose. C-15/C-16/C-18 left untouched, pending Part 10 as instructed. Pushed (`779ee81`).
- **Part 9b — split research off `mill-flash`; found the daily-budget reset window.** Founder's rationale: a single research pass (~$1.50) against 150 brainstorm exchanges/month (~$0.60) meant sharing a key would let one `/test` starve every founder's interactive use for the rest of the day. Created `mill-research` (new key, `models: ["flash"]`, $3.00/`1d`) via `/key/generate`; reduced `mill-flash` to $0.50/`1d` and rescoped it to `models: ["flash-fast"]` only via `/key/update` (existing value preserved). Verified the scoping is actually enforced, not just configured: `mill-flash` → `flash-fast` succeeds, `mill-flash` → `flash` now correctly 403s ("key not allowed to access model"), `mill-research` → `flash` succeeds. `ops/slack-bot/llm.js`'s `callFlash` updated to pick the key automatically from a `model → env var` map rather than always using `MILL_FLASH_KEY` regardless of which model was requested — the old code would have silently tried to call `flash` through the now-rescoped `mill-flash` key and 403'd the first time `/test` gets built, since nothing enforced the pairing structurally before this. Verified end-to-end through the real `callFlash` and `runAttack` after the change (with `max_tokens: 4096`, avoiding the known thinking-token variance that makes small token budgets an unreliable test — one test at `max_tokens: 100` hit exactly that, 97/100 tokens went to reasoning, `content: null`, not a routing bug). `build-guide.md` updated in three more places that had silently drifted from the real, already-edited `~/stack/litellm/config.yaml`: Part 7.2's `config.yaml` snippet still showed the pre-split single `flash` entry (never updated when the `flash-fast` split was made directly against the live file), Part 7.3's key-generation JSON, and Part 9.2's env template. Also fixed Part 11's `research.py` snippet, which pointed `OPENAI_API_KEY` at `MILL_FLASH_KEY` — would have 403'd against the `flash` model the moment Part 11 actually got built, since research always needed `mill-research`, not the interactive key.

  **Budget reset window, checked directly rather than assumed:** `/key/info`'s `budget_reset_at` field reads `"2026-08-27T00:00:00+00:00"` for a key created and last updated at different times during the day (`created_at` 11:14 UTC, `updated_at` 21:04 UTC) — if the window were rolling 24h from either of those, the reset time would follow one of them. It follows neither; it's fixed at the next UTC midnight. **Confirmed: UTC midnight, i.e. 05:30 IST, not rolling 24h from key creation** — exactly the bad case the founder was checking for. An evening exhaustion (e.g. 8pm IST) blocks that key until 5:30am IST the next day, not a few hours later. Documented in `build-guide.md` Part 7.3 directly; no code or config change made in response to this finding — recording it is what was asked for.
- **Part 9b — `mill-research` headroom, budget healthcheck, drift convention.** `mill-research` raised $3.00 → $4.00/1d via `/key/update` for headroom against the 05:30 IST reset window found last pass. Built `ops/healthcheck.sh` (the budget-warning half of Part 12's healthcheck; disk/mem/service checks still pending): queries all four keys' `spend`/`max_budget` via `/key/info` (master-key-authenticated — the only script on the box that holds `LITELLM_MASTER_KEY`, kept out of the always-on bot's own env deliberately), posts to `#mill-ideas` the first time any key crosses 70% of its daily budget on a given UTC day, de-duplicated via a state file per alias/date so it doesn't re-alert every 30 minutes. Tested for real, not just read: ran with all keys under threshold (silent, correct), then forced `mill-flash` over 70% by temporarily lowering its budget to $0.15 against its real ~$0.11 spend — got a real Slack post at 76%, confirmed a second run didn't re-alert, then restored the budget and cleaned up the test state file. Installed via `crontab`, `*/30 * * * *`. `docs/build-guide.md` Part 12 rewritten to match what's actually built rather than the old broken sketch (which used `/global/spend/report`, Enterprise-only, 402s on this build) — also fixed Part 13's checklist line, which still said "three virtual keys" and referenced the same broken endpoint.

  Added a standing convention to `CLAUDE.md`: any change made directly against live config must update the corresponding `build-guide.md` section in the same commit. Four drift instances happened before this was written down (the `flash-fast` split never reaching Part 7.2's snippet, Part 7.3's stale budget JSON, Part 9.2's env template, Part 11's wrong key reference) — all found and fixed only because each was specifically asked about, not because anything caught them structurally.
- **Part 9b — `/think`, `/cross`, `/blindspot`, `/themes` built and verified together.** Extracted shared infrastructure first rather than duplicating per command: `context.js` (`readProfile`, `readDynamics`, `readCaptures` with the `docs/COMMANDS.md`-specified 20-entry/~8000-token cap for most commands vs. `/themes`'s uncapped 30-day window) and `eval-event.js` (the Layer 2 event builder, factored out of `attack.js` so the field set can't drift command-to-command). `attack.js` refactored to use both, re-verified unaffected after (`parseAttackResponse` still correct on both branches).

  `/think`: context `[1][2][4]` (system + profile + last-20-captures), posts the full response to `#mill-ideas`. `/blindspot`: context `[1][3]` (system + `shared/dynamics.md`), deliberately no individual profile per D-27. `/themes`: last 30 days of the sender's own captures, no idea-text argument. `/cross`: the interesting one — **exactly two separate model calls**, per spec, each using a *different* founder's profile (never the sender's own); no third call computes convergence or divergence, since `docs/COMMANDS.md` caps it at two calls and that judgment is for the founders reading both outputs (`runbook.md`'s "Reading /cross" guidance), not something to fabricate from a call that doesn't exist.

  **Verified `/cross` actually reaches the right profiles, not just that it runs**, per the founder's specific ask: wrote two deliberately contrasting dummy profiles for `amisha` (obsessively fixated on CAC/LTV, dismissive of anything without a spreadsheet) and `vaibhav` (obsessively fixated on virality/network effects, dismissive of B2B sales motions) — designed to be unmistakable in output if and only if each reading actually used its named founder's profile. Ran `/cross` as `saksham` against the GST/SaaS idea: the "through amisha's lens" reading was unmistakably CAC/LTV-fixated ("Show me the CAC:LTV ratio... the math doesn't work"), the "through vaibhav's lens" reading was unmistakably virality-fixated ("Where is the viral loop?... Forget selling seat licenses"), correctly attributed to the right name, not swapped or blended. Confirms `otherFounders()` correctly excludes the sender and that each call's profile injection is isolated from the other. Cleared both dummy profiles back to 0 bytes immediately after — confirmed via `git status` showing no diff against their original empty state.

  All four commands' telemetry verified in the real `telemetry/*.jsonl` output with correct `stage` names and, for `/cross`, `tokens_in`/`tokens_out` correctly summed across both calls rather than only the last one. Service restarted and deployed.
- **Part 9b — `/cross` fabrication confirmed and guarded.** Founder asked to re-run `/cross` with both co-founder profiles genuinely empty (their real, current state) and report verbatim, specifically to check whether the model fabricates plausible-looking readings from nothing rather than saying so. It does: two full, confident, ~500-word critiques came back, independently converging on the same generic objections (ClearTax/Tally already do this, CA price sensitivity, GSTN API fragility, flagging-isn't-solving-the-human-chasing-problem) — indistinguishable from a working `/cross` unless you already knew both profiles were blank. Added a guard in `cross.js`: `readAs()` now checks profile length before calling the model at all (`MIN_PROFILE_LENGTH = 50` chars after trim); below that, it returns a placeholder ("...hasn't been recorded yet -- no reading generated from nothing") with zero tokens spent, no model call made. `runCross()` also omits the convergence/divergence closing line when either side was skipped, since comparing a real reading against a placeholder (or two placeholders) isn't a convergence signal. Verified three states: both empty (two placeholders, no convergence line, `tokensIn/Out: 0 0`), one real + one empty (real reading generated, convergence line correctly omitted), and confirmed via the earlier pass that two real profiles still produce the full compare-and-converge output. Test profile content cleared back to 0 bytes each time, confirmed via `git status`. **Same fabrication risk exists in `/think`**, which also falls back to an inert `"(no profile recorded yet)"` placeholder string rather than skipping the call — not fixed here, flagging it since the founder's ask was scoped specifically to `/cross`.
- **Part 9b — `/test` built: field-evidence phase real end-to-end, research pass honestly stubbed.** `ideas.js` extended with `readState`, `readIdeaMd`, `readAssumption` (parsed back out of `idea.md`'s `## Assumption` section — the single source of truth, not duplicated into `state.json`), and `updateState`. New `thread-wait.js`: a small in-memory registry (`thread_ts` → pending resolver) letting a command post a question and await a specific founder's reply in that thread, since Bolt registers message handlers once at startup rather than per-invocation. Wired into `index.js`'s generic message handler ahead of the DM-capture logic.

  **Phase 1 (field evidence) is real**, per the founder's explicit ask: posts the verbatim `docs/COMMANDS.md` prompt to `#research` as a thread root, waits up to 30 minutes (`MILL_TEST_FIELD_TIMEOUT_MS`, overridable — nobody should have to wait 30 real minutes to test the timeout path) for the asking founder's reply in-thread. A non-`none` reply is written to `ideas/<id>/field/notes-<stamp>.md`; `none` or a timeout proceeds web-only.

  **Found and fixed a real bug while testing this**, not a hypothetical: `runFieldEvidencePhase` was matching replies against the founder's *internal name* ("saksham") instead of their actual Slack `user_id`, but `message.user` in a real Slack event is always the raw ID. Every real reply would have silently failed to match, and `/test` would have hit the 30-minute timeout on every single invocation regardless of whether the founder actually responded — the bug was invisible in casual review because the wrong-mechanism failure and the true-timeout path produce the same downstream `evidence_basis: web-only`, just via a broken path instead of the intended one. Caught because a direct test of the "none"-reply case unexpectedly showed `reason_code: field_evidence_timeout` in telemetry when it should have resolved almost instantly. Fixed by passing `command.user_id` through instead of the mapped founder name; re-verified all three paths against a throwaway test idea (never committed) using the lower-level functions directly: real field evidence (resolved in ~0.2s, notes file written), `none` (resolved in ~0.2s, no file), true timeout with no reply sent at all (resolved at exactly the configured timeout, `timedOut: true`). One earlier `/test` run against the real `be67` idea was affected by this bug before the fix (committed as `83171ed`) — its `research-*.md` content is still accurate (the file's own wording covers both the `none` and timeout cases identically and honestly), only the telemetry `reason_code` was mislabeled; left as historical record rather than reverted, consistent with how earlier genuine test artifacts in this repo were handled.

  **Phase 2 (the research pass) is honestly stubbed, not faked.** `docs/COMMANDS.md` specifies it runs `ops/research.py` (Part 11, GPT Researcher) — not built in this deployment. Rather than fabricate web evidence or citations to look complete, the report's "Web evidence" section states plainly that no search ran and no sources were retrieved, and asserts nothing else — D-20 ("no claim without a retrieved source") means the honest move here is silence, not plausible-sounding filler. The one real model call this phase makes is the D-33 gap-output: when `evidence_basis` is `web-only`, a real `flash` call (medium thinking, `mill-research` key — the actual research routing from two passes ago) generates three genuine, non-fabricated resolving questions with who-to-ask, verified to ask rather than assert (no factual claims, so no D-20 risk). When field evidence exists, this call is skipped entirely (`evidence_basis: field-supported`, zero tokens) since the gap output only applies to thin/absent field evidence per spec.

  State correctly transitions to `researched` on completion (`ideas.updateState`), committed via the same `git.js` used by `/attack`. Telemetry emits `stage: "test"` with `evidence_basis` populated (the first command to actually use that field) and `reason_code: "field_evidence_timeout"` distinguishing a real timeout from an explicit `none`. Service restarted and deployed. **Holding here for acceptance before starting `/audit`**, per the founder's explicit one-at-a-time instruction.
- **Part 9b — profile-length guard extended to `/think`; `research_stub` made unmissable in `/test`.** `MIN_PROFILE_LENGTH`/`hasProfile` moved from `cross.js` into the shared `context.js` (was about to duplicate/drift otherwise). `/think`'s system prompt explicitly asks the model to "attack from the angle this founder's profile says they would miss" — with no profile, this swaps to a variant prompt that never asks for that framing, and the reply is prefixed `_(no profile recorded yet...)_` so the founder knows it isn't personalized. Verified against `saksham`'s own real (empty) profile. `/attack` reviewed too: its system prompt never claims profile-derived framing, so the existing `"(no profile recorded yet)"` context marker already avoids the fabrication pattern — no code change needed there.

  `/test`'s `research-*.json` now carries `"research_stub": true` explicitly (not just prose in the `.md`), and the report and completion message both state plainly that `/audit` will refuse to rule on it.

- **Part 9b — `/audit` built: C-07 enforced in code, `research_stub` gate enforced in code, both before any model call where possible.** New `audit-llm.js`, structurally isolated per D-10 ("Fable 5 runs at the audit gate and nowhere else") — it's the only file in the codebase that reads `MILL_AUDIT_KEY` or calls the `audit` model; nothing else imports it. `ideas.js` extended with `readLatestResearch` (most recent `research-<stamp>` pair by lexicographic stamp ordering).

  **Two gates enforced in code, never left to the prompt, exactly as asked:**
  1. **`research_stub` check, before any model call.** A missing report refuses ("no research has run, use `/test` first"); a report with `research_stub !== false` also refuses — deliberately not `=== true`, since every report in this deployment predates the flag being added and absence should never be read as "real research happened." Verified against `be67`'s pre-flag report (refuses) and a from-scratch idea with zero research (refuses, different message). Zero tokens spent on both.
  2. **C-07 (`enforceEvidenceGate`), after JSON parsing, unconditionally.** `verdict === "proceed" && evidence_basis === "web-only"` is downgraded to `narrow` regardless of what the model returned — the system prompt already asks for this, but a prompt instruction is not enforcement; this function is what actually guarantees it. Unit-tested directly against all four relevant combinations before any live call: proceed+web-only → downgraded to narrow; proceed+both → unchanged; kill+web-only → unchanged (kill doesn't need the gate); narrow+web-only → unchanged. `parseAuditResponse` also validates the full required-field set and enum values (not just "is it JSON") — a verdict object missing `verdict` is exactly as useless as unparseable text, so it's treated as the same failure class and triggers the same one retry.

  **Found a real bug on the first live call, not hypothetical:** `docs/COMMANDS.md` shows the audit's JSON schema as a separate documentation block *after* the four-line system prompt — sending only the prose (as literally "verbatim from the system prompt" would suggest), Fable 5 reasonably invented its own field names (`rationale`, `evidence_summary`, `what_would_change_this`) instead of the ones `parseAuditResponse` validates against, and both attempts correctly failed to parse and correctly refused to post an unvalidated verdict — the fail-safe worked, but the root cause needed fixing. Added an explicit `SCHEMA_INSTRUCTION` built from the same `REQUIRED_FIELDS`/`VALID_VERDICTS`/`VALID_EVIDENCE_BASIS` constants the parser itself uses, so the instruction sent to the model and the validation applied to its response can't drift apart. Re-ran live: real `kill` verdict, correctly parsed, correctly triggered `state.json` → `killed`, a `minds/<founder>/graveyard.md` append, `#graveyard` post, and `audit-<stamp>.json` written — full pipeline confirmed end-to-end against a real Fable 5 call.

  **Test-data hygiene:** the live pipeline test used a throwaway idea (`yyyy`) with a manually-constructed non-stub research report, since Phase 2 doesn't produce a real one yet — needed to exercise the actual model-calling path rather than just the refusal paths. Fable 5 correctly killed it (the assumption was a placeholder, genuinely untestable), but that verdict and its graveyard entry weren't real data. Removed in a separate, clearly-labeled commit (`88bea9a`) rather than left in place, since `graveyard.md` is read by founders and by `EVAL.md`'s Layer 3 review — fabricated entries there are a different order of problem than a stray idea directory.

  Service restarted and deployed. All seven of eight commands now implemented; only `/proto` remains.
- **Part 10 — Docker sandbox built and independently verified before `/proto`.** Built `~/stack/sandbox/` (Dockerfile, `run.sh`, `mill-sandbox:latest` image, the `egress` opt-in Docker network) exactly per the existing spec, then ran the documented leak tests against the real container rather than trusting the spec was sufficient — per the founder's framing, "the container's job is to contain a misbehaving agent, so neither of us should accept the agent's own assurance that it works."

  **Two real bugs found while verifying, not hypothetical:**
  1. `curl` was never installed in the image, but the documented network tests use it. Without it, both network tests report `clean`/`BLOCKED-UNEXPECTEDLY` regardless of actual network state — `curl: command not found` exits non-zero and falls through to the `||` branch either way. A false pass, not a real one. Caught because the egress-opt-in test showed `BLOCKED-UNEXPECTEDLY`, which should have been the tell. Re-verified with `python3 -c "import socket; ..."` (genuine DNS failure on the default network, genuine connection on egress) to get a real signal before fixing `curl` and re-confirming the documented tests pass for real with it present.
  2. `mktemp` defaults scratch directories to mode `700`, owned by the host user running `run.sh`. The container always runs as a fixed uid (`10001`), which never matches, so the container could not write anything into its own scratch mount — would have silently broken every prototype's actual output before this was ever used for real. Fixed with `chmod 777` on the fresh per-invocation directory.

  **C-15/C-16/C-17 raw verification output** (the founder asked to re-run these by hand afterward, so reported verbatim rather than summarized):
  ```
  TEST 1 (env leak): clean
  TEST 2 (/home/agent listing): ls: cannot access '/home/agent': No such file or directory
  TEST 3 (mill env read): cat: /home/agent/.config/mill/env: No such file or directory
  TEST 4 (default network, curl): clean
  TEST 5 (egress opt-in, curl): reaches-network
  EXTRA (interfaces, default): lo
  EXTRA (interfaces, egress): eth0, lo
  EXTRA (read-only root): touch: cannot touch '/etc/test-write': Read-only file system
  EXTRA (scratch write, post-fix): scratch-write-ok
  EXTRA (capabilities): CapInh/CapPrm/CapEff/CapBnd/CapAmb all 0000000000000000
  ```
  **C-17 specifically:** confirmed at the interface level, not just behaviorally — the default-network container has *only* `lo` (no `eth0` exists at all, not merely blocked), and the egress opt-in genuinely provisions `eth0` with a real route only when `MILL_SANDBOX_NETWORK=egress` is explicitly set. Nothing reaches the egress network by accident.

  `run.sh` extended with `MILL_SANDBOX_SCRATCH_DIR` (falls back to a fresh `mktemp` dir if unset) so `/proto` can pre-populate a scratch directory with a generated artifact before executing it, without duplicating the sandbox's security configuration in a second code path.

- **`/proto` built and verified against all four required behaviors, end-to-end, against a real throwaway idea.** New `sandbox.js`: the only function in the codebase that executes generated content, always through Part 10's `run.sh` (D-06/D-29 isolation, mirroring `audit-llm.js`'s D-10 isolation pattern). `parseProtoResponse` requires a `FILENAME: <name>` header (not specified by `docs/COMMANDS.md`, added since some parseable contract is needed) and rejects path separators/`..` outright — a generated filename can't write outside its own touch directory.

  - **Refuses without a named assumption:** verified — `/proto <id>` alone, or `/proto` with no id/assumption at all, produces the exact refusal message, no model call.
  - **Refuses on killed ideas:** verified against a real idea manually set to `state: killed`.
  - **Caps at five touches:** verified for real — five consecutive successful touches against a throwaway idea (touch 5 correctly warned "the next /proto ... will be refused"), then a 6th attempt correctly refused with the exact verbatim cap message from `docs/COMMANDS.md`, no model call.
  - **Executes only inside the sandbox:** verified both directions on real model output. A technical assumption ("a checksum validator correctly flags ≥90% of malformed GST invoice numbers") produced real Python, genuinely executed via `sandbox.js`/`run.sh` inside the container, with real output (correctly flagged 18/19 malformed test numbers) captured and posted. A non-technical assumption (a landing-page click-through rate) produced `index.html` with no execution attempted at all — confirmed no `output.txt` was written for that touch, since `.html` isn't in the executable-extension map.

  Found and worked around one real testing-environment artifact (not a code bug): an interactive shell testing this command directly showed `permission denied` connecting to the Docker socket, while the actual deployed `mill-chat` systemd process does not — confirmed directly by reading `/proc/<pid>/status` for the running service's `Groups:` line against `id agent`, both showing `docker` (gid 988) already present. The discrepancy was the interactive shell's stale group cache from when `agent` was first added to the `docker` group (Part 5), not anything wrong with `sandbox.js` — subsequent tests were run via `sg docker -c` to match the real service's environment.

  Two throwaway test ideas (`dead`, `prot`) used across this verification were removed afterward, consistent with how other pure test scaffolding was handled elsewhere in this build. Service restarted and deployed. **All eight commands from `docs/COMMANDS.md` are now implemented.**
- **Part 11 — GPT Researcher built and verified with a real pass; the $1.50/pass estimate was never measured, now it is.** Added a Gemini embedding model (`embed`, `gemini/gemini-embedding-001` — current model name verified against Google's own docs before adding, not the older Vertex-path `text-embedding-004`) to LiteLLM, scoped to `mill-research`, confirmed directly against the proxy's `/embeddings` endpoint (real 3072-dim vector returned) before wiring it into anything. `EMBEDDING=openai:embed` closes the gap flagged last pass — hybrid mode (D-33's actual mechanism) now has a real embedding path instead of silently failing.

  `python3-venv` installed, `gpt-researcher==0.16.0` installed into `~/venv`. **Found a real packaging bug in the PyPI release, not a config issue**: `gpt_researcher/actions/query_processing.py` uses `Any`/`List`/`Dict` in a function signature defined *before* the lines that import them — a `NameError` on the very first `import gpt_researcher`, unconditionally. Confirmed already fixed on the project's `master` branch (same imports, just moved above the function). Rather than install from git master and risk pulling in other unreleased instability, applied the identical one-line reordering fix directly to the installed copy, verified with `py_compile` before retrying.

  `ops/research.py` written with the full spec: env-var setup (unchanged from last pass' fixes), a real citation re-check (D-20 — re-fetches a sample of sources, asks `flash` to decompose into sub-questions per source rather than judge holistically, appends discrepancies under `## Citation issues`), and gap output (D-33 — three resolving questions when `evidence_basis` is `web-only`, generated by `flash`, asserts nothing so carries no D-20 risk). `docs/build-guide.md` Part 11 rewritten to point at the real file rather than duplicate it inline, per the drift convention this build kept needing.

  **Real pass, real assumption, real field notes** (a throwaway test idea, `resc`, removed afterward — never committed): "At least 15% of Indian CA firms managing 50+ clients are willing to pay more than ₹15,000/year for a standalone GST input-credit mismatch tool over the built-in reconciliation features already included in their ClearTax subscription," with a genuine-sounding field-notes file describing two CA-partner interviews.

  **All four required checks verified directly against the real output, not assumed:**
  - **Sources returned:** 17 URLs in `research-<stamp>.json`'s `sources` array.
  - **`research_stub` goes `false`:** confirmed in the written JSON (`/audit`'s `research_stub` gate, built before this existed, will now actually let a real report through).
  - **Citation re-check actually re-fetches:** confirmed by the results themselves, not just that code ran — two sources came back HTTP 403 (real blocked re-fetch attempts) and one came back genuinely `UNSUPPORTED` (fetched content lacked the substance to back the report's claims), all three appended under `## Citation issues` in the report. A check that always says "supported" would have been the tell that it wasn't real; this one wasn't.
  - **Hybrid mode reads the field-notes file:** `evidence_basis: "both"` (only reachable when field notes exist), and the report body cites `notes-manual.md` as a named source seven separate times across multiple sections, correctly listed in the References section as a "Local Document."

  **Cost, measured precisely via LiteLLM's own `LiteLLM_SpendLogs` table for the exact run window, not GPT Researcher's self-reported estimate** (which reported $0.241 — almost certainly calibrated for OpenAI GPT-4o-mini/4.1 pricing, not our actual Gemini rates, and not used as the reported figure for that reason): **$0.0706 total** — 3 `flash` calls (report writing + my own citation-check/gap-output calls) plus 8 `embed` calls, 71,228 input tokens / 12,483 output tokens combined. **The $1.50/pass estimate in the original budget was roughly 20x too high** — first real measurement against the actual configured models (Gemini 3.7 Flash + Gemini embeddings), not the estimate's era. Tavily's own search cost is separate and not tracked by LiteLLM (Tavily bills directly, not through the proxy) — see below.

  **Tavily: plan "Researcher", 1000 credits/month, key-level limit unbounded (capped by the plan).** Checked directly via Tavily's own `/usage` endpoint, not assumed. Usage showed 0 across the board immediately after this run despite real search activity in the report (current, dated sources were found and cited) — likely a reporting lag on Tavily's side, noted as observed rather than resolved.
- **Resolved: Tavily's zero-usage reading was a dashboard reporting lag, not a silent fallback.** Founder correctly refused to accept "probably a lag" without checking — traced it properly rather than re-asserting the assumption. First check was misleading: an isolated `TavilySearch.search()` call incremented the `/usage` counter from 0→6 within seconds, which looked like real-time tracking and implied the original full-pipeline run's 0 reading was real. But several more *confirmed-genuine* search calls after that (2+ second latency each, topically correct on-topic results for deliberately distinct novel queries) left the counter frozen at exactly 6 for several minutes — proving the counter isn't synchronous, and the earlier 0→6 jump was coincidental batch timing, not evidence either way. Settled it conclusively by monkeypatching `requests.post` to trace actual outbound HTTP calls during a real `GPTResearcher.conduct_research()` run: **5 real POSTs to `https://api.tavily.com/search`, all HTTP 200.** Also confirmed via GPT Researcher's own `research` logger (`Active retrievers: ['TavilySearch']`) that the correct, only-configured retriever is what's active. The 17 sources and $0.0706 cost from the original pass stand as reliable — Tavily was genuinely hit both times; its own usage dashboard just updates on a delay, not in step with requests. Two more test ideas (`rcvt`, plus assorted isolated diagnostic runs) used for this investigation — none of the diagnostic runs were committed.
- **Part 12 — weekly profile evolution, weekly cleanup cron; 15-min git batch commit re-confirmed already working from Part 9.** Built `ops/slack-bot/profile-evolution.js` and `weekly-scheduler.js`, wired into `index.js`.

  **Design departure from the original cron sketch, made deliberately, not by accident:** `docs/build-guide.md` Part 12 originally sketched profile evolution as `ops/profile_diff.py` on a `30 9 * * 0` cron line. Built instead as an in-process interval inside the always-on `mill-chat` bot, because D-30's approve/reject flow needs Socket Mode's persistent Slack connection to post interactive buttons and later receive the click — a separate cron-invoked script would need its own bot session just for that. `weekly-scheduler.js` polls every 5 minutes for a Sunday 04:00–04:10 UTC window (09:30 IST, no DST — server timezone confirmed via `timedatectl` earlier in the build, not assumed) and fires once per ISO week, tracked via a marker file so a mid-window restart can't double-fire. `profile-evolution.js` generates one `flash-fast` diff per active founder's `profile.md` (last 7 days of captures + graveyard + current profile) and one shared `dynamics.md` diff (all active founders' captures combined), posts each as a Slack message with Approve/Reject buttons — DM for a profile diff, `#mill-ideas` for the shared one. `index.js`'s new `profile_diff_approve` action handler is the only code path that writes the proposed content, and it only runs on an explicit human click (D-30); reject just deletes the pending diff (`minds/<founder|shared>/.pending-diffs/`, gitignored) and logs the outcome via `emit()`/`buildEvalEvent()`.

  **Found and fixed a real failure mode via direct testing, not assumption:** `generateProfileDiff('saksham')` against real (currently near-empty) data threw `"flash-fast call returned no content"`. Diagnosed via a raw curl to LiteLLM with the same prompt shape — `finish_reason: "stop"` (not `"length"`), `content: null`, `reasoning_tokens: 153`, `text_tokens: 0`: Gemini 3.x had reasoned over the sparse prompt and genuinely chosen not to emit visible text, a different failure mode from the previously-documented reasoning-budget-exhaustion case. First attempt at a fix was too broad — changing `llm.js`'s `callFlash` to never throw on empty content, so all seven other commands would keep their token/cost usage on this path too. Reverted after checking those callers directly: `themes.js`/`blindspot.js`/`cross.js`/`think.js`/`test.js`/`proto.js` all treat an empty `content` as valid text and would have silently posted blank output to Slack instead of surfacing a real failure, which `llm.js`'s throw currently and correctly prevents. Fixed narrowly instead: `profile-evolution.js`'s own `callFlashForDiff` catches only that specific error message and returns `content: null`, which both diff-generator functions treat identically to a genuinely-unchanged proposal (`changed: false`) — consistent with D-30's "small diffs are correct," which implicitly allows "no diff" as a legitimate outcome for a sparse week.

  **Verified directly, not assumed working:**
  - `generateProfileDiff('saksham')` against real (sparse) data: `{ changed: false, tokensIn: 0, tokensOut: 0, ... }`, no throw.
  - `generateDynamicsDiff()` against real data: same, no throw.
  - A direct `callFlash` call with a prompt engineered to produce no text reproduced the exact `"call returned no content"` error, confirming the caught path is real and not hypothetical.
  - A temporary real capture appended to `minds/saksham/captures/`, then removed after the test (never committed — `git status --short minds/` confirmed clean afterward): `generateProfileDiff('saksham')` correctly returned `changed: true` with a well-formed unified diff proposing a new `profile.md` section.
  - `mill-chat` restarted to deploy; `journalctl`/`/home/agent/logs/chat.log` show a clean boot with no errors from the new scheduler or action handlers.

  **Weekly cleanup cron installed** (was not previously in `crontab`, only `healthcheck.sh` was): `docker system prune -af --volumes` and a scratch-directory prune (`find ... -mindepth 1 -maxdepth 1 -mtime +7`, corrected from the original guide's `-type d` sketch, which would have silently skipped stray files — matches the `proto-<id>` directory names `/proto` actually creates), both Sunday 03:00/03:05 UTC, logging to `~/logs/cleanup.log`. Confirmed `agent` can run `docker` unprivileged (`getent group docker` shows `agent` a member; `sg docker -c "docker system df"` succeeded) — matches Part 10's `run.sh`, no sudoers change made or needed. (My own interactive shell showed `permission denied` on a bare `docker system df` — same stale-group-cache artifact already documented for `/proto` in the Part 9b entry above, not a real problem for a freshly-spawned cron process.)

  **15-minute git batch commit for captures (`git-batch.js`, Part 9) re-confirmed already running** — no new work needed, already verified in an earlier pass.

  `docs/build-guide.md` Part 12 rewritten to match what's actually built (per the drift convention), replacing the stale `profile_diff.py`/`eval.py` cron sketch with the real design.
- **Part 12b — EVAL.md Layer 1 conformance script, all 22 checks.** `ops/conformance.py`, no dependencies beyond the stdlib (`urllib` instead of `requests`, matching `healthcheck.sh`'s zero-extra-dependency pattern). Every check is a grep, file read, git command, or HTTP call to LiteLLM's own `/key/info`/`/spend/logs` endpoints — never a model call, per EVAL.md's explicit "do not ask a model what a script can prove."

  **Found and fixed one real bug in the script itself while testing against real data, not left in place:** C-17's first version grepped `run.sh`'s own comment text for a specific sentence documenting the known egress-allowlist gap (Part 10) and would have silently passed, because the comment line-wraps across two lines in the actual file and a single-line substring search missed it. Rewrote to check the real thing instead of the comment describing it — `docker network inspect egress` plus `iptables -L DOCKER-USER -n` — which is more robust anyway (can't drift out of sync with a comment someone rewords later). Also found and fixed a false positive in C-13: the founder-directory rename commit (`c7f3732`, `minds/amit` → `minds/saksham` etc.) showed up as a second, unapproved commit to `profile.md` under `git log --follow`, but `git show --numstat` confirms it's a pure rename with 0 content changes — fixed to only flag commits with actual content diffs against the path, not every commit `--follow` walks through.

  **Full run against the real repo: 21/22 pass.** The one genuine failure and two adjacent findings surfaced by building this, none fixed here since none are mine to resolve unilaterally (structural per CLAUDE.md's own rule — raise with founders, don't act alone):

  1. **C-17 fails for real.** The `egress` Docker network has no `DOCKER-USER` iptables rule restricting destinations — confirmed directly, not assumed. Matches what `run.sh`'s own comment already admits (Part 10): a binary none/egress toggle, not a per-host allowlist. Close before any working prototype needs live network access unattended.
  2. **This system never invokes Pi at all.** Grepped `ops/` for any `pi -p`/RPC/spawn usage — none. Every command handler and `ops/research.py` talk to LiteLLM's `/chat/completions` directly over HTTP. D-03 named Pi as the harness; CLAUDE.md's pitfalls table says `pi -p --mode json` for stage runs and RPC for the bot. Pi (Part 6) has never been more than orchestrator scaffolding in the actual running system. C-18 passes on its literal wording (no Pi call *outside* a container, because there's no Pi call anywhere) but that's a technicality, not evidence the intended architecture is in place — recorded plainly rather than let the passing check imply otherwise.
  3. **`eval-event.js`'s `buildEvalEvent` mis-prices every non-Gemini telemetry event.** It always uses `geminiFlashCost()` regardless of the `model` field, so every `stage: "audit"` line has been logging Fable calls at Gemini's per-token rate since Part 9b — silently wrong. Confirmed via LiteLLM's own `/spend/logs` (correct built-in Anthropic pricing) and the `x-litellm-response-cost` response header, either of which could replace the hardcoded calculator. C-02 avoids inheriting this bug by reading `/spend/logs` directly for Fable MTD spend rather than trusting telemetry's `cost_usd`, so C-02 itself is accurate — but D-24's "cost per idea killed" metric, which does read telemetry, has been getting wrong numbers for the audit stage this whole time.

  `docs/build-guide.md` gets a new Part 12b documenting the script and these three findings inline, so they don't live only in this log entry.

**Per the founder's instruction: stopping here. Part 13 (commissioning) is the founder's, not this session's.**

---

## Founder responses to Part 12b's findings

Three explicit decisions came back on the three findings Part 12b surfaced. All three are done; this session stops here per the founder's instruction — Part 13 (commissioning) is theirs.

**1. Pi: removed, not wired in.** `npm uninstall -g @earendil-works/pi-coding-agent` run — confirmed removed. `docs/DECISIONS.md` D-03 marked SUPERSEDED (header and a pointer to D-43 at the top; the original dsh-vs-Pi comparison body kept verbatim below it, per "never delete, mark superseded," since the founder specifically asked for the reasoning to stay instructive even though its conclusion no longer drives anything). New D-43 added: LiteLLM's `model_list` already supplied the model-agnosticism Pi was chosen for, the custom Bolt bot already supplied the Slack surface (D-39 was never really `pi-chat` to begin with), and Part 10's sandbox already supplied execution isolation — nothing was left for a harness to do. D-04, D-06, D-15 got small corrections to stop describing Pi as a live constraint (D-06's core reasoning is unaffected — `sandbox.js` executes generated code directly either way, noted explicitly rather than left implicit). D-39 got its own correction: it originally claimed the bot "shells out to `pi -p --mode json` per message," which was never true even before D-43 — the bot always called LiteLLM's `/chat/completions` directly. `CLAUDE.md`, `docs/runbook.md`, `docs/build-guide.md` (Part 6 marked historical/skip-on-fresh-build, target line and Part 9 mention fixed) all updated to match. `ops/conformance.py`'s C-18 comment/result text updated to describe the real, current reason it passes (Pi is gone) rather than the technicality it passed on before (Pi was never called despite being installed).

**2. Telemetry pricing fixed at the source, not patched over.** The bug: `eval-event.js`'s `buildEvalEvent` always computed `cost_usd` from `tokens_in`/`tokens_out` via a hardcoded Gemini-only rate table (`pricing.js`), regardless of which model actually ran — every `stage: "audit"` (Fable 5) event was silently priced at Gemini rates instead of Fable's real cost. Fixed by reading LiteLLM's own `x-litellm-response-cost` response header (real, per-model, computed by LiteLLM itself, confirmed byte-for-byte against a live `/spend/logs` row for the same call) in `llm.js`'s `callFlash` and `audit-llm.js`'s `callAudit`, returning it as `costUsd` alongside `content`/`usage`. `eval-event.js`'s `buildEvalEvent` no longer computes cost at all — `costUsd` is now a required-in-spirit parameter (defaults to 0, the honest "unknown" value for a call that threw before returning), threaded through by every caller. `pricing.js` deleted — nothing imports it anymore.

  Threaded `costUsd` through all nine call sites that needed it: `attack.js`, `audit.js`, `cross.js` (two parallel calls summed, matching its existing `tokensIn`/`tokensOut` aggregation pattern), `test.js`, `think.js`, `blindspot.js`, `proto.js`, `themes.js`, and `profile-evolution.js` (including its `callFlashForDiff` wrapper — the "model reasoned but emitted no text" case now also carries real `costUsd`/`usage` via `llm.js` attaching them to the thrown error, instead of silently reporting 0 for calls that genuinely spent tokens).

  **Backfilled the two real historical audit-stage events with nonzero cost** in `telemetry/2026-08.jsonl`, matched to LiteLLM's `/spend/logs` by exact token count (a more reliable join key than timestamp alone, since one event was two application-level retry attempts summed): the `malformed_verdict_after_retry` event corrected from `0.0062325` to `0.04155` (sum of two 275-prompt/776-completion attempts, one of which LiteLLM recorded at exactly `0.0` spend — a real LiteLLM-side quirk, not a backfill error), and the `kill`-verdict event corrected from `0.0026295` to `0.03506` — roughly 13.3x, matching the founder's estimate almost exactly. Verified directly (not assumed): a live test call through the fixed `callAudit` returned `costUsd: 0.00036`, and the matching `/spend/logs` row for that exact call showed `spend: 0.00036` — exact match, byte-for-byte.

  **Added C-23** to `docs/EVAL.md` (new "Telemetry accuracy" subsection under Layer 1) and `ops/conformance.py`: samples recent cost-bearing telemetry events and checks `cost_usd` against the sum of LiteLLM `/spend/logs` rows falling inside each event's own call window (`ts - wall_clock_s - buffer` to `ts + buffer`) for the matching `model_group` — summing rather than single-nearest-match, since a correctly-summed multi-call event (like `/cross`'s two parallel reads) will always exceed any one row and a naive nearest-single-row match would wrongly flag it. Scoped to events after a hardcoded `PRICING_FIX_DEPLOYED_AT` cutoff (the `mill-chat` restart timestamp) rather than judging pre-fix historical data against a standard it predates — those events are deliberately not in scope (only the two audit ones were backfilled, per the founder's specific instruction), and C-23 would otherwise fail permanently on data nobody asked it to correct. Verified end-to-end: generated a real post-fix `/think` call directly through the fixed code path, confirmed C-23 passed against it, then removed that synthetic telemetry line afterward so it doesn't sit in the real operational record as a founder action that never happened.

  Redeployed (`systemctl restart mill-chat`) partway through this work specifically to establish the cutoff timestamp and get the fix live before validating C-23 against it.

**3. C-17 stays failing, deliberately.** No change made to weaken the check or the underlying gap. It fails because the `egress` Docker network genuinely has no per-host allowlist — confirmed via `docker network inspect` and `iptables -L DOCKER-USER -n`, not assumed. **Status: failing, and should stay that way until a prototype actually needs unattended network access** — at which point closing the gap (a real per-destination allowlist on the `egress` network, not just the current none/egress binary toggle) becomes a prerequisite for that prototype, not optional hardening.

**Full conformance run after all three responses: 22/23.** Only C-17, as intended.

---

## Post-reboot: DM routing wedge, observability hardening, cache-hit cost fix

- **DM routing bug diagnosed as a wedged Socket Mode connection, not a code bug.** After the 2026-08-27 reboot the `mill-chat` process reconnected into a half-open WebSocket: it stayed `active` for ~5 hours while silently dropping inbound `message.im` events (founders' plain-DM captures in that window are lost — Slack does not redeliver `message` events) and Slack retried slash-command deliveries, producing duplicate `#mill-ideas` posts (visible in telemetry: `/think` fired twice 2s apart at 16:04 IST, the second a 0.037s LiteLLM cache hit). `Restart=always` never fired because nothing exited; `@slack/socket-mode`'s own auto-reconnect logged the pong failures (`[WARN] bolt-app A pong wasn't received...` all over `chat.log`) but did not recover. A plain `systemctl restart mill-chat` cleared it — verified twice with live DMs afterward (`routing test 1`/`2` → attributed capture + ✅, nothing posted anywhere).
- **`ops/slack-bot/socket-health.js` — a wedged socket is now a dead process (D-44).** Started from `index.js` after `app.start()`. Exits non-zero (→ `RestartSec=10`) on: websocket pong stale >90s (via the `undici:websocket:pong` diagnostics channel, the same one socket-mode subscribes to internally); >3 consecutive pings with no pong; or no inbound Slack event for 15 min *and* a live `auth.test` probe also failing (the probe stops a quiet-but-healthy overnight connection from restart-looping). Maintains `~/logs/mill-chat.heartbeat` (rewritten every inbound event and every 60s). Thresholds are env-overridable (`MILL_SOCKET_*`, `MILL_HEARTBEAT_FILE`). Verified live: `pong_age_s=1` steady, `NRestarts=0`, heartbeat ticking on the 60s timer.
- **Permanent `[inbound]` event log in `index.js`.** One line per dispatched Slack request (type, channel kind, user, text length — no message content). There was previously zero logging of what Slack delivered, which is the only reason the wedge needed live instrumentation to diagnose. Founder asked for this kept permanently.
- **`ops/healthcheck.sh` rewritten from budget-only to four checks + always-log.** Per-key daily budget (unchanged), `mill-chat` not `active`, heartbeat stale >10 min, root disk >85%. **Every run appends one `[<utc>] healthcheck ran — <summary>` line regardless of outcome** — silent success was how the old version hid whether cron was running it at all (the founder's "sixth silent failure this build"). Alert de-dup moved from per-day to per-clock-hour so a persistent problem re-pings hourly. `LITELLM_MASTER_KEY` was **not** actually missing — it's sourced from `~/stack/litellm/.env`, which the script already did; the script ran fine, just invisibly. Verified the alert path by forcing a stale-heartbeat condition: real post to `#mill-ideas`, then deleted.
- **`ops/conformance.py` C-24 added** — asserts `~/logs/healthcheck.log` has a fresh `[<utc>] healthcheck ran` line within 24h. `docs/EVAL.md` gets a new "Observability" subsection for it.
- **Channel env vars converted from `#names` to IDs** in `~/.config/mill/env` (`#mill-ideas`→`C0BSWSKRGG1`, `#research`→`C0BSQVAD6A1`, `#graveyard`→`C0BSUTZ1E1Y`), resolved via post-and-delete since the bot lacks `channels:read`. `config.js` now logs a warning for any channel value that isn't `C…`/`G…`-shaped. Name resolution still works in `chat.postMessage` but breaks silently on a rename and isn't accepted by `conversations.*` (which is why `conversations.history` was failing during diagnosis). `build-guide.md` Part 9.2's env template — which never listed `SLACK_CHANNEL_*` at all — now does, as IDs. Secret-bearing `env.bak.*` from the edit was `shred`ded.
- **Cache-hit cost bug fixed at the source (`llm.js`, `audit-llm.js`).** LiteLLM's `x-litellm-response-cost` header reports the *would-be uncached* price even on a cache hit, while billing nothing (`/spend/logs` row for a hit shows `spend: 0.0 cache_hit: True`, key-spend unchanged). Since the brainstorm prefix is cached deliberately (`config.yaml` `cache: true`), cached calls are the common path, so trusting that header over-reported `cost_usd` on most brainstorm events — this is what produced the one C-23 mismatch. Fix: detect the hit via the `x-litellm-cache-key` response header (present only on a hit, verified miss-vs-hit), record `cost_usd: 0` and a real `cache_hit_ratio`. Threaded `cacheHit`/`cacheHitRatio` through all nine call sites (`eval-event.js` now takes `cacheHitRatio` instead of hardcoding `0.0`; `cross.js` averages across its two reads; the retry-loop commands count hits/calls). Verified through the deployed `callFlash`: miss → `cacheHit:false costUsd:0.000351`, hit → `cacheHit:true costUsd:0`, matched against `/spend/logs`. The single already-logged mismatch line in `telemetry/2026-08.jsonl` was corrected to `cost_usd:0, cache_hit_ratio:1` with a `note`. C-23 extended: additionally fails if any `cache_hit_ratio > 0` event carries nonzero `cost_usd`.
- **`ops/slack-bot/git.js` now rebases before pushing and surfaces failures to `#mill-ideas` (D-45).** The bare `git push origin main` was silently failing whenever `origin` moved (which it does constantly — founder pushes plus the bot's own capture batches and per-idea commits), leaving commits local and captures/ideas out of the shared repo D-21/D-38 depend on, with only a `console.error` nobody reads. Now `fetch` + `rebase origin/main` + `push`; a rebase conflict aborts cleanly and retries next batch; every failure posts via a new `notify.js` (raw `chat.postMessage`, mirrors `healthcheck.sh`). Verified in production: the bot's batch loop committed and pushed `routing test 1`/`2` to `origin/main` through the new path with no alerts; `notify.js`'s post path tested directly (post + delete).
- **Conformance after all of the above: 23/24.** Only C-17, unchanged and still deliberate.
- Two secondary items flagged, not actioned: the LiteLLM `config.yaml` cache TTL / whether audit responses should ever be cache-served (they can be, and D-10's "one pass" intent might argue against it — left for the founder); and `telemetry/*.jsonl` being gitignored, so telemetry corrections and the monthly review's data live only on the box (consistent with how it's been handled, not changed).

- **Batch-commit scope fixed (the `b28a714` cause).** `git.js`'s `commitAndPush` now runs `git add -- <paths>` **and** `git commit -m msg -- <paths>` — the pathspec on `commit` is the fix: a bare `git commit -m` records the whole staged index, so anything a founder had `git add`ed while working in the same clone got committed by the bot under "captures: batch commit" (which is how the first half of the previous session's work landed in a batch commit). `git commit -- <paths>` fences everything else out; `git add -- <paths>` is still needed to pick up new untracked files (a fresh `ideas/<id>/`). The rebase step gained `--autostash` so it survives a founder's in-flight unstaged edits instead of aborting. Verified in a scratch repo: bot commits only `minds/` + new `ideas/<id>/`, a founder's staged `docs/x.md` and untracked `docs/y.md` both survive, push succeeds.
- **CLAUDE.md conventions** gained two rules, per the founder: every periodic/background component emits a log line every run (pass or fail); every alerting fix is verified by forcing the condition. Recorded in the repo, not just session memory — six silent failures this build, all the same shape.

---

## Projects phase

- **Part 14.1 — MiniMax / `mechanical` tier removed (D-46, supersedes D-12).** `mechanical` model entry deleted from `~/stack/litellm/config.yaml`; `mill-mech` virtual key `/key/delete`d; `MILL_MECH_KEY` removed from `~/.config/mill/env`; `MINIMAX_API_KEY` removed from `~/stack/litellm/{.env,docker-compose.yml}` and the container recreated (`docker compose up -d`), `/health/readiness` healthy, `/v1/models` now `['flash-fast','flash','audit','embed']`. `flash-fast` verified still working through the proxy. Document indexing (Part 17) will use `flash-fast`. Code/doc refs updated: `healthcheck.sh` (key list), `conformance.py` (C-04 regex, C-05 expected keys), `research.py` (comment), `CLAUDE.md`, `runbook.md` (budget line folds into buffer), `PROJECTS.md`, `EVAL.md` (C-04 text), `build-guide.md` Part 0.2/7.2/7.3/13 (struck through, D-46 pointer, so a from-scratch build skips it — three keys not four). Conformance 23/24 after (only C-17).
- **Part 14.2–14.7 — chat tier built.** New modules: `chat-session.js` (in-memory session registry keyed on `thread_ts`, mirrored to `~/.cache/mill/chat-sessions/*.json` outside the repo so a restart doesn't lose raw thinking; `buildContextMessages`, `maybeCompact`, `commandDestination`, `postCommandResult`, `drainForNightlyCapture`), `chat-turn.js` (conversational-turn handler, wired into `index.js`'s message router *before* the DM-capture path so the two never collapse — 14.3), `tavily.js` (direct Tavily client for `/search`), `commands/chat.js` (`/chat <topic>` opens a `#chats` thread + session), `commands/search.js` (`/search` — 1–3 planned queries, `flash-fast` summary, mandatory not-evidence footer, 14.5), `nightly-capture.js` (20:00 UTC in-process job appending each founder's own chat turns to their captures file as `(chat) …`, 14.7 — in-process not cron for the same reason weekly profile evolution is: the state lives in this process).
  - `commands/attack.js`: in `#chats` (`channel_id === SLACK_CHANNEL_CHATS`) it runs the model and posts the case + `ASSUMPTION:` line into the chat thread but **creates no idea / state.json / directory** (14.4) — verified: `ideas/` unchanged, telemetry `reason_code: "chat_no_idea"`, `idea_id: null`.
  - `commands/{think,cross,blindspot,themes}.js`: destination now resolved via `commandDestination(command)` — threads into the founder's active `#chats` session when invoked there (slash commands carry no `thread_ts`, so the active session is found by owner + channel), otherwise the invoking channel, falling back to `#mill-ideas`. Command exchanges in a session are recorded as turns so compaction/promotion keep them.
  - `config.js`: `SLACK_CHANNEL_CHATS` added to the channel map.
  - **Compaction (14.6):** at 30 turns, `flash-fast` summarises all but the last 10, prompt explicitly requires preserving every assumption/number/named-alternative; a visible marker is posted; `session.summary` grows, `compactedThrough` advances. Full transcript is untouched (kept for promotion).
  - **Verify Part 14 — automated, raw output in the session:** [1] 5-turn chat recalled a price+quantity stated at turn 3 ("₹1,499 per month for 8 litres"); [2] a second concurrent session recalled only its own topic/number (25000/yr), no bleed from session 1; [3] compaction fired at exactly 30 turns, `compactedThrough`→20, 10 kept verbatim, summary preserved "349" and "Swiggy" from turn 3; [4] `/search` output carried the not-evidence footer; [5] `/attack` in `#chats` created nothing in `ideas/` and no git change; [6] DM→capture path unchanged (message router checks chat-turn first, returns false for DMs) and was live-verified earlier this session; [7] nightly drain returns only the owner's user-role turns and is flush-tracked (second drain empty). Conformance 23/24 (only C-17).
  - **Blocked for live use, flagged to founder:** `/chat` and `/search` must be registered as slash commands in the Slack app config (api.slack.com/apps → Slash Commands) — build-guide-projects 14.0 doesn't list this and it can't be done without Slack app-admin access. Plain-message conversational turns work without it; starting a session via `/chat` does not.
- **Part 15 — promotion (chat -> project).** New: `promote-button.js` (the "Start a project from this idea" Block Kit action; own zero-dep module to avoid a require cycle), `promotion.js` (`promoteChat`, `extractAssumption`, `summarizeChat`, `postNeedsProject`), `ideas.js` `promoteIdea` (writes `idea.md` + `origin-chat.md` + `state.json`; assumption optional; `state.json` gets `origin:"chat"`, `origin_chat_ts`, `assumption`, and the Part-16 placeholders `channel_id`/`threads`/`parent`/`children`).
  - **15.1 button** attached to every bot reply in a chat thread: conversational turns (`chat-turn.js`), `/attack` in chat, and `/think`/`/cross`/`/blindspot`/`/themes`/`/search` output that lands in a session (via `postCommandResult`). `index.js` gets the `promote_chat` action handler.
  - **15.2 triggered prompts:** `/test`, `/proto`, `/audit` invoked from `#chats` don't run — they `ack`, post "That needs a project" + button into the session thread, emit `reason_code: "needs_project"`. A file upload in a `#chats` session thread (`chat-turn.js`, before the plain-text path) posts "I can't store files in a chat — start a project" + button. Never silent.
  - **15.3:** all fallible work (transcript build, assumption extraction, `flash-fast` summary, and — via the `_simulateFailure` seam — Part 16's future channel creation) happens *before* `generateIdeaId`/`promoteIdea`, so a failure creates no idea. On success: `promoteIdea`, `commitAndPush(["ideas/<id>"])`, `markPromoted`, seed summary + assumption line to `#mill-ideas`, confirmation into the chat thread. (Project channel + Brainstorm thread are Part 16; the seed goes to `#mill-ideas` until then, per 15.3's "flat structure" note.)
  - **Verify Part 15 — automated, raw output in session:** [A] promoting a 40-turn chat (compaction had fired at 30) wrote `origin-chat.md` with all 40 turn markers and no "Compacted turns" text — the full transcript, not the compacted view; [B] a `/attack` assumption from turn 5 carried into `state.json` as `assumption` + `has_assumption:true`, `origin:"chat"`; [C] `_simulateFailure` threw before any write — `ideas/` unchanged, session not marked promoted; [D] an upload in a chat returned the button-bearing "start a project" reply, `consumed=true`, no model call. Conformance 23/24 (C-17 only).
  - Test idea `0552` (created by exercising the real `promoteChat`, which calls `commitAndPush` for real) removed in this commit, matching how earlier verification-scaffolding ideas were handled.
- **Part 16 — project channels (D-47).** New `project-channel.js`: `createProjectChannel` (`conversations.create` `#idea-<id>-<slug>`, invite all active founders, `setTopic` = assumption, post the 5 stage anchors and return their `thread_ts`), `slugify`/`channelName` (Slack's `[a-z0-9-]`/≤80 rules), `COMMAND_STAGE` map, `repostAnchors`. `ideas.js`: `promoteIdea` now takes `channelId`/`threads`; `findIdeaByChannel` (reverse lookup by `state.channel_id`); `setAssumption` (rewrites idea.md's `## Assumption` + state).
  - **16.1/16.2:** `promotion.js` `promoteChat` creates the channel *before* writing any idea files, so a `conversations.create` failure creates no idea (verified via `_simulateFailure`); on success it seeds the **Brainstorm** thread with the origin-chat summary + a jump link, announces `<#channel>` in `#mill-ideas`, and confirms in the chat thread.
  - **16.3 routing:** `chat-session.js` `commandDestination` now recognises project channels and returns `{channel, threadTs: state.threads[stage], project, stage}`; `ensureStageThread` reposts all anchors and re-persists `state.threads` if a stage `thread_ts` is missing/stale (never posts to channel root). `/think`/`/cross`/`/blindspot`/`/themes` route via `postCommandResult`; `/attack` in a project posts to Brainstorm and sets the idea's assumption if unset (doesn't create a second idea); `/test` takes the id from the channel and threads the field-evidence prompt + report into the Research stage thread; `/audit` posts the verdict into the Audit stage thread (kills still cross-post to `#graveyard`); `/proto` takes the whole arg as the assumption, posts into the Prototype thread.
  - **Per-thread conversation isolation:** `getOrCreateStageSession` lazily creates a session keyed on each stage thread's `ts` (`kind:"project"`, `promoted:true` so nightly capture skips it). A plain message in a project stage thread is a conversational turn scoped to that thread — `chat-turn.js` generalised to handle both `#chats` and project channels.
  - **16.4:** `#research` retired for new work (D-47). `runbook.md` / `COMMANDS.md` updated; pre-projects ideas with no `channel_id` (e.g. `be67`) still fall back to `#research`. `conformance.py` had no channel-name reference to change.
  - **Verify Part 16 — automated, raw output in session (real channel created via the bot, then archived):** [1] `#idea-zz16-…` created with all 5 stage anchor threads, the active founder a member, topic = the assumption; [2] `/think`→brainstorm, `/test`→research, `/audit`→audit, `/proto`→prototype thread; [3] deleting `state.threads.research` then routing `/test` → `ensureStageThread` reposted anchors, wrote a fresh `thread_ts` back to state, never returned undefined; [4] a "RESEARCH FINDING … ZEBRA-9" turn added to the Research thread session was absent from the Brainstorm thread's built context. Conformance 23/24 (C-17 only). Test channel archived, `ideas/zz16` removed.
- **Part 17 — documents.** New `documents.js` (`handleProjectUpload`), wired into `index.js`'s message router on `subtype:"file_share"` — no new Slack event subscription needed (file uploads arrive on the already-subscribed `message.channels`). Project channels only (`findIdeaByChannel`); posts into the Documents stage thread.
  - **17.1 capture:** downloads `url_private` with the bot token immediately (retry once on failure, then post the filename to the Documents thread and store nothing — "never silently drop"), writes to `ideas/<id>/docs/`, `-2`/`-3` collision suffix never overwrites, `commitAndPush(["ideas/<id>/docs"])` attributed to the uploader, ✅ reaction (best-effort).
  - **17.2 size:** `>20 MB` refused with an ask-for-an-extract message, nothing downloaded; `5–20 MB` accepted with a repo-bloat warning; `<5 MB` accepted.
  - **17.3 indexing:** `flash-fast` appends a `## <name>` / uploader·date·size / Type / Summary(~150w) / Key figures block to `ideas/<id>/docs/index.md`. `.txt/.md/.csv/.json/...` extracted directly; `.pdf`/images sent to Gemini as an inline data-URI; `.docx/.xlsx` and any extraction failure → raw file kept, index entry marked "extraction failed".
  - **17.4 context (`context.js`):** `parseDocMention` splits a leading `@token`; `readDocIndex` / `readProjectDocs`. `/think` in a project channel now includes `docs/index.md` by default, `@filename <q>` includes that file's full text for that call, `@all <q>` includes everything (400 KB cap). Audit is unaffected (D-28 keeps its context to assumption + research report). `/test` passing `docs/` as GPT Researcher `DOC_PATH` is **pending the real research-pass integration** — the bot's Phase 2 is still the honest stub from Part 9b, so there's no `research.py` invocation to hand `DOC_PATH` to yet.
  - **Verify Part 17 — automated, raw output in session (mocked Slack client, injected downloader):** [C] a 25 MB `.pdf` was refused ("over the 20 MB limit"), not written; [E] a forced download failure (after the retry) posted the filename + "not stored", wrote nothing; [A] a `.md` upload was stored, ✅-reacted, and got a real `## q2-market.md` index entry with Type/Summary — and `/think` with no mention answered from the index (go-to-market discussion), not the raw body; [B] `/think @q2-market.md …` answered "monthly subscriber churn is **6%**" straight from the document body; [D] a `.docx` (no converter) was stored raw with its index entry marked "extraction failed". Conformance 23/24 (C-17 only).
  - Test idea `zz17` (its `docs/` were committed by the real `commitAndPush` inside `handleProjectUpload`) removed in this commit.
- **`/search` → `/find`** (Slack reserves `/search`). `commands/search.js` → `commands/find.js` (`handleFindCommand`, `runFind`, `STAGE="find"`); `index.js` registration; all `/search` strings in `chat.js`/`tavily.js`/`chat-session.js` comments; `docs/COMMANDS.md` (new `/chat` + `/find` entries), `docs/PROJECTS.md`, `docs/build-guide-projects.md`. Behaviour unchanged: not-evidence footer, transcribes as conversation on promotion. No conformance check referenced the command name. `/chat` registered fine by the founder.
- **Part 18 decisions recorded (D-48), pre-build.** No static host — `STATIC_HOST_TOKEN` dropped from the env template and the static-host path removed from `docs/PROJECTS.md`/`docs/build-guide-projects.md`; every prototype goes through the single ngrok slot. Two distinct sandbox network profiles instead of one none/egress toggle: **build** = allowlist `registry.npmjs.org` only (npm install is build-time), **mount** = deny-all + DNS (a publicly-reachable LLM-written app has no other legitimate egress). C-17 must pass once Part 18 is built. D-48 amends D-06 and the Part 10 single-`egress`-network design.

---

## Projects Part 18 — lifecycle and mountable prototypes

- **Two sandbox network profiles (D-48), enforced in `DOCKER-USER`.** `~/stack/sandbox/net-setup.sh` creates `mill-build` (172.30.0.0/24) and `mill-mount` (172.31.0.0/24), deletes the old unrestricted `egress` net, and rebuilds `DOCKER-USER`: ESTABLISHED/RELATED RETURN, DNS (udp+tcp 53) RETURN for both subnets, tcp/443 to `104.16.0.0/13` (npm's Cloudflare range) RETURN for **build only**, then catch-all `-j DROP` per subnet. `mill-sandbox-net.service` (oneshot, `After=docker.service`, enabled) applies at boot; a `docker.service` drop-in (`ExecStartPost`) re-applies after a daemon restart flushes the chain. `~/stack/sandbox/run.sh` now takes `MILL_SANDBOX_NETWORK=none|build|mount`. `refresh-npm-allowlist.sh` is the tripwire for npm leaving Cloudflare.
  - **Verified positively with python sockets (per the founder's ask — not "no error"):** from a `mill-mount` container, `create_connection(("1.1.1.1",443))` → *timeout*, `example.com:443` → *Network unreachable*, `93.184.216.34:80` → *timeout*; `gethostbyname("example.com")` *resolves*, `1.1.1.1:53` *connects*. From `mill-build`: `registry.npmjs.org:443` *connects*, `1.1.1.1:443` *fails*, `example.com:80` *fails*.
  - **C-17 rewritten** to assert `DOCKER-USER` has a catch-all DROP for each sandbox subnet and no non-DNS accept for `mill-mount`; **now passes**. C-15/C-16 extended to cover `mount.sh` (allows `-e PORT=8080` explicitly). **Conformance 24/24.** `sudoers.d/mill-sandbox`: `agent` may `iptables -S DOCKER-USER` and run `net-setup.sh`.
- **ngrok service — traffic-policy syntax verified against current docs.** `type: basic-auth` under `on_http_request[].actions[].config.credentials: ["user:pass"]`, inline under `endpoints[].traffic_policy` — `ngrok config check` accepts it, no CLI-flag fallback needed. Config is **rendered from `~/stack/ngrok/ngrok.yml.tmpl` via `envsubst`** (`render.sh`, chmod 600) because ngrok v3 doesn't reliably interpolate `${VAR}` and the token/password mustn't live in a committable template. `mill-ngrok.service` built (`ExecStartPre=render.sh`), **disabled** — the handed-over `NGROK_AUTHTOKEN` (`ep_…`, ~30 chars) is an API-key/endpoint-token, not a valid agent authtoken (~49 chars, no prefix). `ngrok start` returns `ERR_NGROK_105`. Flagged; everything else is built and waiting on the real token.
- **Mount slot (`ops/slack-bot/mount.js`, `~/stack/sandbox/mount.sh`).** One slot on `127.0.0.1:3200`. `mount.sh {up|down|idle|status}`: exactly one of `mill-proto-mount` / `mill-proto-idle` (64 MB placeholder, "no prototype mounted", never 502) binds 3200. Mount container: `--user 10001 --cap-drop ALL --read-only --tmpfs /tmp -v scratch:ro --memory 512m --cpus 0.5 --pids-limit 128 --network mill-mount --env-file /dev/null -e PORT=8080`. `/proto` result posts get a `[Mount touch N]` button in the Prototype thread; `index.js` handles `proto_mount`/`proto_dismount`/`proto_extend`/`proto_takeover`. Default 30 min, `Extend` +30, **8 h cap**, 5-min warning with `[Extend]`. Contention → `[Take over]` (dismounts the held one with a note in its thread, no queue, no auto-evict). **18.7:** `ngrokPublicUrl()` hits the Agent API before any URL is posted; if unconfirmed it says so rather than reporting a URL on assumption; also does a local `GET :3200/` check.
  - **Restart reconciliation tested for real (per the founder's ask):** created a real project channel + `ideas/zz18` + `proto/1/index.html`, mounted it (`mill-proto-mount` up, serving on 3200, `state.json.mount` set), `systemctl restart mill-chat` → log: `mount: reconcile -- zz18 still mounted, re-arming timers` / `{"state":"mounted","id":"zz18","running":true}`. Then killed the container behind the bot's back and restarted again → `zz18 state says mounted but no container is running; clearing state` / `{"state":"stale_cleared"}`, `state.json.mount` → null, 3200 back to the idle placeholder. The bot trusts `mount.sh status`, never `state.json` alone. Test channel archived, `ideas/zz18` removed (never committed).
- **18.1 archive on kill:** `/audit` in a project channel, on a `kill` verdict, `conversations.archive`s the channel after posting; failure logs + alerts `#graveyard` and the verdict still stands.
- **18.2 `/spinoff <idea>`** (`commands/spinoff.js`, registered): project-channel only; creates a child project (channel + idea via `promoteIdea`, no origin chat), records `parent`/`children` both ways, links back in both Brainstorm threads and announces in `#mill-ideas`. Needs Slack slash-command registration.
- **D-04 amended** (the ngrok tunnel is the one public-exposure exception; fenced by basic-auth at the edge + the D-48 mount profile + single slot + 8 h cap). **D-06 amended** (long-running mount containers; same isolation as the one-shot path plus deny-all egress; reconcile against the running container). `docs/build-guide-projects.md` Part 18.3/18.8 and `docs/EVAL.md` C-15..C-17 rewritten to match what's built.

---

## IMPROVEMENTS I1 + I2 — graded field evidence, outcomes at dismount (D-49)

- **I1 — the audit grades field evidence.** `evidence_basis` is now a 5-value grade the *audit* assigns from the raw notes: `none` / `web-only` / `field-intent` / `field-behaviour` / `field-committed` (weakest→strongest). `/test` and `ops/research.py` no longer grade — they emit `web-only` or `field-raw` (ungraded). `commands/audit.js`: `SYSTEM_PROMPT` gains the grading rubric ("intent = they said they would; behaviour = observed spend / a real workaround / a price named unprompted; committed = paid, pre-ordered, signed up"); `enforceEvidenceGate` (C-07, in code after JSON parse) downgrades `proceed` to `narrow` for `none`/`web-only`/`field-intent` — `proceed` needs `field-behaviour` or `field-committed`. The audit reads the **raw** `field/notes-*.md` (via a new `readFieldNotes`) so it grades the founder's unedited text, not their framing. `/test` field prompt rewritten to ask for behaviour, not intent (surveys overestimate WTP ~21%).
  - **Verify (real Fable calls):** intent-only notes ("all three said they'd definitely use it, 15k sounds reasonable") → model graded `field-intent`, verdict capped at `narrow`. Behavioural notes ("runs an Excel macro 6h/month, pays a freelancer 12000/yr, asked unprompted 'when can I buy this'") → model graded `field-behaviour`, and the gate did **not** downgrade it. Deterministic gate check: `proceed`+{none,web-only,field-intent} → `narrow (downgraded)`; `proceed`+{field-behaviour,field-committed} → `proceed`; `kill` never touched.
- **I2 — capture outcomes at dismount.** `mount.js` `dismount` now (non-blocking) posts an outcomes prompt in the Prototype thread — "who saw it, what did they *do*, anything about price" — including the ngrok request count for the mount window (`localhost:4040/api/requests/http`; "0 requests" is stated as its own finding, "unknown" if the inspector is unreachable). The founder's threaded reply is written to `ideas/<id>/outcomes.md` (`appendOutcome`, timestamp + founder + duration + request count), committed, and confirmed. `readOutcomes` feeds `outcomes.md` into audit context alongside the research report and raw field notes — a recorded signup/payment there is what makes `field-committed`.
  - **Verify:** mount state with `mounted_at` 34 min ago + a stubbed inspector returning 2 requests → dismount prompt said "2 requests" and "Who saw it?"; a threaded reply wrote to `outcomes.md` with the founder + duration + count; a subsequent `runAudit` with `outcomesMd` in context graded `field-behaviour`.
- **Docs:** D-49 added (supersedes D-33's 3-value `evidence_basis`); `COMMANDS.md` (`/test` phase-1 prompt, audit context + schema + hard-validation), `runbook.md` (Stage 3 prompt, Stage 4 output), `EVAL.md` (C-07 wording, Layer 2 example, the `field-behaviour` share metric + a new `field-intent` share metric). `ops/conformance.py` C-07 extended to flag `proceed` on `none`/`web-only`/`field-intent` (and the pre-I1 `field-supported`). **Conformance 24/24.**

---

## IMPROVEMENTS: git.js test-idea exclude + I3 + I4 + I5

- **Test artifacts no longer swept into commits.** `git.js`: `commitAndPush` appends `:(exclude)ideas/zz*` (no `glob` magic, so `*` crosses `/` and children are excluded too) to every `git status`/`add`/`commit`. `generateIdeaId()` now rejects any id matching `/^zz/i`, keeping the real (4-char hex) and test (`zz*`) namespaces disjoint. Verified: with only `ideas/zzI4/` changed, `commitAndPush(["ideas"])` is a no-op and `ideas/zzI4` stays untracked. This ends the pattern of a cleanup commit after every verification run (four times in Parts 14–17, again with zzI2).
- **I3 — auditor reads the graveyard.** `ideas.js` `readGraveyardDigest()` parses all three `minds/*/graveyard.md` into a compact `id (founder, date): assumption / killed: reason` list (cap 40, reasons ≤240 chars). `commands/audit.js`: added to `runAudit` context; `SYSTEM_PROMPT` gains the resemblance instruction; `resembles_killed_idea` added to `REQUIRED_FIELDS` / `SCHEMA_INSTRUCTION` / `parseAuditResponse` (string or null) and shown in the verdict post. **Verify (real Fable call):** planted a `kk01` graveyard entry (thali meal-kit, killed for last-mile cold-chain economics), audited a reworded near-duplicate → `resembles_killed_idea: "kk01"`, `strongest_failure_reason` cited the distribution/last-mile reason.
- **I4 — staleness nudge.** New `ops/slack-bot/staleness.js`: daily in-process sweep; an idea at `open`/`researched` unchanged for 14 days → one nudge in the project channel with a `[Kill it]` button (`stale_kill` action, reason `stale`); again at 30; then stops. Thresholds tracked in `state.json.stale_nudges`. `[Kill it]` → `state: killed` + graveyard append + `#graveyard` post + channel archive + commit. `appendToGraveyard` exported from `audit.js` for reuse. **Verify:** 15d → 1 nudge, `stale_nudges [14]`; same day again → 0; 31d → nudge, `[14,30]`; 45d (both done) → 0; `killStale` → `killed` + graveyard `stale` entry.
- **I5 — India-anchored research prompt. PARTIALLY VERIFIED.** `ops/research.py`: `_INDIA_RE` (keywords + `₹` + `Rs N`) sets `INDIA_ANCHORED`; when set, the GPT Researcher `query` is prefixed with the directive (prioritise India sources / ₹ pricing / local regulatory context; a US comparable is context not evidence), the report gets a marker line, and `india_anchored: true` goes in the research JSON.
  - **Verified:** the *detector* only. It fires on "Bangalore CA firms / ClearTax", "Rs 1499/month", "₹1499/mo", "Pune hardware startups"; it does not fire on "Berlin SaaS founders" or a non-market technical assumption.
  - **NOT verified:** the actual effect — a real research pass producing a report that cites Indian sources with ₹ pricing rather than US comparables presented as evidence (IMPROVEMENTS.md's stated verify step). That needs the real `/test` → `research.py` integration (still the honest stub in the bot) and a real India-anchored assumption. Do this as part of the post-month-of-use review, alongside the pending Tavily-vs-Exa comparison.
- **PENDING (I5, per founder instruction): Tavily vs Exa retriever comparison.** IMPROVEMENTS.md asks to run three real assumptions through both via `ops/search.py`'s swappable interface and compare India relevance. **Not run** — deferred until there are real assumptions from actual use; running it on invented inputs would decide the wrong thing.
- D-50 records I3/I4/I5 + the "deliberately not adopted" list. `COMMANDS.md`/`runbook.md`/`EVAL.md` updated (audit schema, audit context, `resembles_killed_idea` output, graveyard-resurrection-caught and `stale`-kill metrics). Conformance 24/24.

---

## EVAL.md Layer 1 audit against I1–I5 (closing task)

Went through every I1–I5 invariant asking "is there a deterministic thing a script could prove, and is it proven?"

- **I1 (graded evidence, `proceed` needs behaviour/commitment)** — covered. `C-07` was updated to fail on `proceed` + `none`/`web-only`/`field-intent`. The "grading is the audit's job, not `/test`'s" sub-invariant isn't worth its own check: the audit prompt is told to ignore any pre-assigned label, so a regression where `/test` grades doesn't actually breach the gate.
- **I2 (outcomes at dismount)** — nothing scriptable beyond code-presence. `outcomes.md` is free text and legitimately absent most of the time; "audit.js reads it" is a grep with weak signal. Not added.
- **I3 (graveyard in audit context)** — **gap found and closed.** `readGraveyardDigest()` can silently return "" if its regex stops matching a reformatted `graveyard.md`, and the audit would then just not catch resurrections with nothing failing. `C-26` added: every kill-shaped line in a graveyard must be extracted by the same regex. Near-vacuous now (graveyards empty); guards the future.
- **I4 (staleness nudge)** — **gap found and closed.** The daily sweep is a scheduled job with no other heartbeat; if it dies, ideas rot un-nudged and nothing complains — the exact silent-failure pattern. `C-25` added: no idea whose full 14-day window elapsed under I4 (cutoff `2026-08-28`, like C-23's) may lack a recorded 14-day nudge.
- **I5 (India research directive)** — nothing scriptable. It's a prompt tweak; the only "check" would be re-implementing the detector regex in the conformance script, which proves nothing. Not added.

Conformance now **26/26** (C-25, C-26 added).

---

## D-51 — commands in threads: `@Mill <cmd>` + one-tap offers

Blocker found in live use: Slack rejects slash commands typed inside a thread ("That slash command is not supported in threads"). PROJECTS.md/COMMANDS.md both assumed `/find /think /cross /blindspot /attack /test /audit /proto /spinoff` run inside chat and project-stage threads; only `/chat` worked (invoked from the channel body). Second unverified Slack-surface assumption after the one-level-of-threads limit — recorded as such in D-51.

- **`ops/slack-bot/intent.js`** (new). `detectRegexIntent()` — clause-anchored regex for unambiguous imperatives ("attack this", "run this past the others"), so the common case never depends on model judgement; narration ("someone could attack this on price") is excluded by a `NOT_NARRATED` lookbehind + clause-start anchor. `parseMention()` — maps the first word after `@Mill` to an action. `validateSuggestion(action, ctx)` — refuses to offer an action that doesn't apply to current idea state (`/audit` before research, `/proto` on a killed idea or at touch 5, project-only command in `#chats`). `splitReplyTrailer()` + `PROMPT_TRAILER_INSTRUCTION` — parse/emit the `---MILL-ACTION---` delimiter trailer (delimiter, not a JSON envelope: escaping a markdown reply into a JSON string field is a reliability problem).
- **`ops/slack-bot/command-shim.js`** (new). `dispatchCommand()` builds the exact `{command, ack, client}` shape the real handlers take and calls them unchanged — no gate re-implemented or bypassed. Captures any `ack({text})` ephemeral refusal and reposts it as a thread message (handlers deliver input-validation refusals — no named assumption, killed idea, idea not found — via `ack`, invisible on a non-slash path otherwise). When a brainstorm command is invoked with no argument, rebuilds the subject from the thread (topic + recent substantive turns, bare imperatives dropped) so `@Mill attack` / a tapped "attack this" offer sees the idea, not the word "attack". `/proto` exempt — still refuses without an explicit assumption.
- **`ops/slack-bot/chat-turn.js`.** One model call, now with `PROMPT_TRAILER_INSTRUCTION` spliced in as a system message. Splits the trailer; intent = regex hit `||` (high-confidence model suggestion). Validates against live idea state, builds an `offer` (with `turnIndex` of the triggering user turn) or records `offerSuppressed`. Only the trailer-stripped reply becomes a session turn — the offer is never a turn, so it can't count toward compaction. Telemetry now logs `suggestedAction`/`suggestionConfidence`/`regexAction`/`offerMade`/`offerAction`/`offerSuppressedReason` on **every** turn including nulls, so an over-eager prompt (offers on >~1/5 of turns) shows up.
- **`ops/slack-bot/promote-button.js`.** `buildReplyBlocks(text, {promote, offer, threadTs, ttlMin})` — section + actions (promote button, `run_suggested` button carrying `{action, threadTs, offeredAt, ideaId, turnIndex}`) + a context hint (`_or say @Mill <action> — this offer expires in 2h_`). `RUN_ACTION_ID`, `OFFER_LABEL` added.
- **`ops/slack-bot/index.js`.** `app.event("app_mention")` — allowlist check, strip `<@ID>`, `parseMention`, `dispatchCommand`; unrecognized → help text. `app.action("run_suggested")` — expires at `MILL_OFFER_TTL_MS` (default **2h**, not 24h — brainstorm threads move in minutes); refuses if the session was promoted, compacted past the triggering turn, or moved >8 turns on; runs against `sess.turns[turnIndex].text`, not "latest". `promote_chat` handler resolves a `"PENDING"` value from the interaction payload's message ts so the button renders on the `/chat` root message.
- **`chat-session.js`** `commandDestination` — `#chats` branch prefers the session for the carried `thread_ts` (an `@Mill`/offer invocation) over "the founder's latest".
- **`telemetry.js`** — `MILL_TELEMETRY_DIR` env override so test runs redirect telemetry to scratch and don't pollute the real log (C-23 reconciles it against LiteLLM spend; rapid-fire test calls otherwise read as mismatches). Test scaffolds `_d51test.js`/`_d51gates.js` set it; `.gitignore` now excludes `ops/slack-bot/_*.js`.

**Gate parity verified** (`_d51gates.js`, real refusals via the `@Mill` path): `@Mill audit` on stub research → refuses; `@Mill proto` with no assumption → refuses (this one regressed mid-build — `ack`-delivered refusal was being swallowed — then fixed); `@Mill proto` at touch 5 → cap refusal; `@Mill audit` with no research → refuses; `@Mill proto` on a killed idea → refuses; `enforceEvidenceGate` (C-07) is called unconditionally inside `handleAuditCommand`, path-independent by construction.

**Behaviour verified** (`_d51test.js`, live `flash-fast`): plain turn → no offer; "attack this idea" → attack offer + promote button both present; incidental "the counterargument is obvious" → no offer; "run this past the others" → cross offer; "research this" in `#chats` → suppressed (`needs_project`); tapping a fresh offer runs `/attack` against the triggering turn and posts the case + assumption.

Also fixed: the promote button was not rendering on bot replies inside `#chats` threads — `chat-turn.js` now routes every reply through `buildReplyBlocks`.

Docs: **D-51** added to `docs/DECISIONS.md`; `docs/PROJECTS.md` (new "Running commands in threads" section + stage-thread / chat-command notes), `docs/COMMANDS.md` (new "Invocation paths" section), `docs/build-guide-projects.md` (14.4 rewritten, 15.2 + 16.3 notes). Bot restarted clean (Socket Mode connected, watchdog armed, 1 session restored). **Conformance 26/26.**

---

## D-52 — one request/one answer, thread-aware commands, feedback, mrkdwn

Five bugs from first real use; four were two design errors (D-52).

**Root Cause A — conversational layer and command layer both answered.** `chat-turn.js` was a straight pipeline: always call the conversational model, always post its prose, then decorate with an offer button. "Now attack this idea" got a prose attack *and* a button to run the real `/attack`. Rewrote `handleChatTurn` as a branch taken *before* the model call:
- `detectRegexIntent` hit + `validateSuggestion` ok → `execute()` (dispatch the command), **no model call**, no prose, no offer.
- model trailer `confidence: "high"` + valid → `execute()`, the prose reply is **discarded, never stored as a turn**.
- `confidence: "medium"` + valid → prose reply **+** one-tap offer (the only path that still offers).
- `low`/`null` → prose only.
`intent.js`'s `PROMPT_TRAILER_INSTRUCTION` rewritten: tells the model not to do a command's job in prose (ack in one line and stop), and defines high/medium/low explicitly with "when unsure, choose low". `_rcab.js` verifies: plain turn → prose no offer; "ok now attack this idea properly" → runs `/attack`, no second prose attack, nothing stored; incidental mention → nothing; loose "part of me wants to see the sharpest case against this" → prose + `medium` offer.

**The medium bar is measured.** Offer button `value` now carries `confidence` (always `"medium"`); `run_suggested` in `index.js` emits an `offer_tap` telemetry event with `offer_tap_confidence`. `eval-event.js` gained `executed_action` / `execution_source` (`regex` | `model_high` | `offer_tap`) / `discarded_reply`. `EVAL.md` Layer 2 adds the medium-offer tap-rate metric — high rate ⇒ the model is calling commands "medium" and the bar is wrong.

**Root Cause B — commands got the message, not the conversation.**
- `command-shim.js`: `dispatchCommand` now assembles `command.thread_context` = recent turns (`MILL_CMD_CONTEXT_TURNS`, 14) + `readOriginContext(ideaId)` for a project stage thread.
- `intent.js`: `isAnaphoric()` / `ANAPHORIC_RE` ("these", "this", "what we discussed", or under ~6 words).
- `commands/find.js`: `resolveTopic()` — when the ask is anaphoric and thread context exists, a `flash-fast` call restates it as a concrete standalone subject before `planQueries`; the posted result shows "you asked: … — resolved from the thread". `/find` also now threads into any session (`getSession(thread_ts)` first), not just `#chats` — it was posting to channel root in project threads.
- `commands/test.js`: `runResearchPass` takes `threadContext`, folded into the gap-question prompt (context, not evidence).
- `chat-session.js`: `readOriginContext(ideaId)` = `idea.md` + length-capped (`MILL_ORIGIN_CHAT_CHAR_CAP`, 24000) `origin-chat.md`; `buildContextMessages` loads it into the cached prefix of every `kind: "project"` session. `_rcab.js` verifies a promoted idea's Brainstorm thread answers "where did we leave off" from the origin chat and does not claim a blank slate.
- `ideas.js`: `readOriginChat(id)` added.

**Bug 1 — no processing feedback.** `chat-turn.js` posts `_Thinking…_` / `_On it — running /X…_` immediately and `chat.update`s it in place. `run_suggested` acks the tap with a visible line. `commands/test.js` and `commands/proto.js` post a breadcrumb across their long stretch. **TOO_VAGUE across promotion:** `promotion.js` `extractTooVague()` scans the transcript; `ideas.js` `promoteIdea({tooVagueDetail})` writes the needed specifics into `idea.md`'s Assumption section and `state.json.assumption_blocked_on`; the Brainstorm seed says what's missing instead of a generic "run `/attack`".

**Formatting — Markdown → Slack mrkdwn.** New `mrkdwn.js`: `toSlackMrkdwn()` converts `**bold**`/`__b__` → `*b*`, `## heading` → `*heading*`, line-start `- `/`* `/`+ ` → `• `, leaving fenced/inline code alone. `wrapClientFormatting()` idempotently patches `client.chat.postMessage`/`update` to convert `text` + mrkdwn block fields — applied to `app.client` and via `app.use` to every listener's client, one choke point. Post-process, not a prompt instruction (the models revert to Markdown regardless). Response length/verbosity untouched.

**Thread scroll position — investigated, reported, not built.** Slack's thread-open scroll is client-side with no API surface; `conversations.mark` only moves the bot's own cursor. What's controllable: `chat.getPermalink` (jumps to a message), `pins.add` + in-place `chat.update`, channel topic. Recommended a pinned per-project "current state" card; **held for an explicit go-ahead** per the founder's instruction to confirm the constraint first.

Docs: **D-52** in `DECISIONS.md`; `PROJECTS.md` / `COMMANDS.md` "Running commands / Invocation paths" rewritten; `build-guide-projects.md` 14.4/14.5/15.3/16.3; `EVAL.md` Layer 2 metrics + schema note. Tests: `_rcab.js` (A/B/Bug1/mrkdwn, all green on live `flash-fast`), `_d51gates.js` gate parity still 6/6, `_d51test.js` units pass. Bot restarted clean. **Conformance 26/26.**

---

## D-52 follow-up — pinned per-project state card

`state-card.js` (new). `upsertStateCard(client, id, {latestTs, latestChannel})`:
- reads `state.json` + `readAssumption` + `readLatestAudit` (new in `ideas.js`), renders a card: `📍 Idea <id> · <state> · <founder>` / assumption (or `blocked on: …`) / last verdict (`verdict — evidence_basis · stamp`) / `Next:` (state → thread-safe `@Mill <cmd>` hint) / `Latest activity:` permalink;
- if `state.state_card_ts` set → `chat.update` in place; on `message_not_found` → repost. Else `postMessage` → `pins.add` → persist `state_card_ts` via `updateState`;
- always refreshes the channel **topic** to `state · assumption` (≤250, `channels:manage` only — already held);
- `pins.add` `missing_scope` → one log line, card still posted (not sticky until `pins:write` is granted);
- no `channel_id` (pre-projects idea) → no-op. Every failure caught internally — a card problem never breaks the command that triggered it. Card text run through `toSlackMrkdwn`.

Wired into every state transition: `promotion.js` (after brainstorm seed; + a second `commitAndPush` of `state.json` because the first commit ran before the channel existed, so a restart in between would otherwise lose `state_card_ts` and double-post), `commands/attack.js` project branch, `commands/test.js`, `commands/audit.js` (**before** archive-on-kill, or the update would hit an archived channel), `commands/proto.js`, `staleness.js` `killStale` (before archive), `commands/spinoff.js` (child).

`state.json` gains `state_card_ts`. **No card for `#chats`** — disposable, single-thread, every reply already carries the promote button (matches the founder's "project case is the one that matters").

**Scope gap flagged:** the bot's token has no `pins:write`. Current scopes: `app_mentions:read chat:write im:write reactions:write commands channels:history channels:read files:read im:history im:read users:read channels:manage`. Until `pins:write` is added the card posts and stays current but isn't pinned; the channel topic (which works now) carries the one-line version.

Tests: `_statecard.js` — 26/26 (nextStep per state; first upsert posts+pins+topic+persists; second updates in place with no re-post/re-pin; vanished target → repost; missing scope → posts + warns once + no throw; channel-less → no-op). Conformance 26/26. `_rcab.js` / `_d51gates.js` still green. Bot restarted clean.

Docs: D-52 thread-scroll paragraph rewritten (built, not deferred); `PROJECTS.md` "The pinned state card" + `state.json` example; `build-guide-projects.md` 16.4.

---

## D-52 amendment — placeholder replacement + /attack thread context

Two bugs from live use in a project Brainstorm thread.

**Bug 1 — placeholder not replaced.** `chat-turn.js` `execute()` updated the "_On it — running /attack…_" placeholder, then the handler posted its result as a *separate* message. Fix:
- **`reply.js`** (new). `postResult(client, msg)` — a handler's one terminal message. `withProgress(client, {progressTs, progressChannel})` — returns a client (via `Object.create`, isolated per dispatch) whose `chat.postResult` redirects its **first** call targeting `progressChannel` into `chat.update({ts: progressTs})`; subsequent calls and other-channel calls (a kill's `#graveyard` post) pass through. Also exposes `chat.progress = {ts, channel}` for multi-message commands.
- **`mrkdwn.js`** `wrapClientFormatting` now also sets a default `client.chat.postResult = postMessage` so the slash-command path (no placeholder) and bare mock clients work unchanged.
- **`command-shim.js`** `dispatchCommand({..., progressTs, progressChannel})`: wraps the client with `withProgress`, sets `command.progress`, reposts a captured `ack({text})` refusal via `postResult` (so gate refusals land in the placeholder too).
- **`chat-turn.js`** `execute()`: always resolves a real `progressTs` (posts the "On it…" line if there wasn't a "_Thinking…_" placeholder) and passes it.
- **`index.js`**: `app_mention` now posts an "On it…" placeholder and passes it; `run_suggested` captures its "On it…" ts and passes it.
- Handlers' terminal posts switched to `postResult`: `chat-session.js` `postCommandResult` (covers think/cross/blindspot/themes), `attack.js` (chat + project branches), `find.js`, `audit.js` (verdict + no-research/stub/malformed refusals; `#graveyard` stays `postMessage`), `proto.js` (result + touch-cap + unparseable; the build breadcrumb edits `command.progress` in place), `spinoff.js` (spun-off notice). `/test` edits the placeholder through its stages (`stageLine`) and posts the report separately — it has a mid-flow interactive wait.

**Bug 2 — `/attack` `TOO_VAGUE` despite full thread context.** Confirmed: `command.thread_context` was populated by the shim but `attack.js` never read it (same miss as `find.js`). Traced: "Let's start by attacking it" isn't a bare imperative (`isBareImperative` → false, no verb immediately after "let's"), so `threadSubject`/blanking didn't fire, and `runAttack` got just that sentence. Fix:
- **`intent.js`** `composeIdeaInput(text, threadContext)` — thread context present ⇒ the idea is the conversation; the trailing message is a pointer, appended only if non-anaphoric and substantive.
- Wired into `runAttack`, `runThink`, `runCross`, `runBlindspot` (each takes `threadContext`, handler passes `command.thread_context`).
- `command-shim.js` `threadContextText(threadTs, channelId)` — now also falls back to `findIdeaByChannel(channelId)` → `readOriginContext` when there's no live session for the thread (`@Mill attack` as the first message in a stage thread).
- `/test` already folded `thread_context` into its gap-question prompt (previous session). `/audit` still deliberately ignores it (D-28).

**`state-card.js`** now commits its own `state_card_ts` write (`commitAndPush(['ideas/<id>/state.json'])` on card creation) — callers commit at varying points, some before the card exists. Removed the redundant explicit commit from `promotion.js`. (This surfaced as `ideas/f05e/state.json` sitting uncommitted after a live run — committed here.)

Tests: **`_bug12.js`** — composeIdeaInput unit; `withProgress` unit (first→update, second→post, other-channel→post); live `/attack` in a project Brainstorm thread with the mechanism/customer/incumbent in-thread → placeholder updated with the result, no separate result message, assumption with a number + named alternative (not `TOO_VAGUE`); `@Mill proto` no-assumption refusal lands in the placeholder. `_rcab.js` / `_d51gates.js` / `_d51test.js` / `_statecard.js` green. Conformance 26/26. Bot restarted clean.

Docs: D-52 amendment in `DECISIONS.md`.

---

## D-52 amendment 2 — questions don't route; momentum bias; classifier-call cost

Live use: "Didn't we also remedy the MoR solution by considering a layer of automation allowing brands to become MoR?" — a recall question — ran a full `/attack` prosecution.

**Interrogative guard.** `intent.js`: `isInterrogative(text)` (turn's main clause leads with a question word after optional filler — `QUESTION_LEAD`), `isExplicitRunRequest(text)` (carve-out: "can/could/would/will/please [you] <run-verb>"), `shouldRouteToCommand(text) = isExplicitRunRequest || !isInterrogative`. `chat-turn.js` computes `routeOk` once and applies it to **both** the regex fast path and the high-confidence model path, for all nine actions uniformly — a question never auto-executes. A blocked question still converses and can still surface a one-tap offer (a blocked `high` is downgraded to a `medium` offer); it just never runs. Telemetry: `interrogative` + `routing_suppressed` on every `chat`/`project_turn` event; `EVAL.md` gets an "interrogative over-routing" metric. Confirmed against `/find` `/cross` `/blindspot` `/test` `/audit` — same code path, all gated. Unit-tested 13/13 phrasings.

**Momentum bias (confirmed and fixed).** The classifier *does* see prior turns including command output — `buildContextMessages` includes all uncompacted turns, and `/attack`/`postCommandResult`/`/find` store their invocation + output as turns. Fix: those turns now carry `kind: "command"` (`addTurn` gained the param); `buildContextMessages` keeps them as reply context but prefixes the assistant ones "[output of a slash-command run earlier — reference only, not a request]", and `PROMPT_TRAILER_INSTRUCTION` now says judge intent from the latest message only, prior command output is not a signal, and a question is not a request unless it explicitly asks to run something.

**C-23 regression fixed.** `chat-turn.js`'s `execute()` returned before the conversational path's own telemetry emit, so on a `model_high` route the classifier/reply call's cost was spent but never logged. C-23 caught the gap on a real `f05e` turn (logged 0.0099 vs 0.0172 spent). `execute()` now takes and logs `modelCostUsd`/`modelTokens…` from the section-2 call. `PRICING_FIX_DEPLOYED_AT` in `ops/conformance.py` bumped to `2026-08-29T10:38:41+00:00` to exclude the pre-fix live window, per the existing cutoff convention.

Tests: **`_interrog.js`** — the exact reported turn on a thread that just ran `/attack` → conversational answer about the prior discussion, `interrogative: true`, `executed_action: null`; controls: "now attack this" and "can you attack this?" both still route. `_rcab.js` / `_bug12.js` / `_d51gates.js` green. Conformance 26/26. Bot restarted clean.

Docs: D-52 amendment in `DECISIONS.md`; "a question is not a request" in `COMMANDS.md` / `PROJECTS.md`; `EVAL.md` metric + telemetry field list.

---

## D-53 — one agent loop above the commands; /proto build loop

The D-51/D-52 intent classifier (regex + `suggested_action`/`confidence` + a growing guard cascade) is replaced by a tool-calling agent loop. Spike first: `flash-fast` tool-calling through LiteLLM, 6/6 on the scenarios the classifier kept failing (recall question, "can you attack this?", imperative, thinking-out-loud), consistent across 4 runs.

**Phase 1 — agent loop.**
- `llm.js` `callFlashTools(messages, {tools, toolChoice})` — same auth/cost/timeout as `callFlash`, returns `{content, toolCalls, finishReason, usage, costUsd, cacheHit}`, doesn't throw on empty content.
- `tools.js` (new) — 9 OpenAI tool schemas + adapters. Each adapter runs `validateSuggestion` as a fast precondition, then `dispatchCommand` (the shim) — no handler re-implemented. Returns `{posted, result}`: `posted` → the command owns the response, agent stops; `!posted` (precondition block / error) → agent loops once and the model relays it.
- `agent.js` (new) — `runTurn({session, message, client})`. System prompt: default to prose; call a tool only on an explicit imperative in the latest message; a question/musing is not a request; prior tool output is context not a signal; one tool per turn, no prose after a tool call. Posts a "Thinking…" placeholder, runs one model step, either dispatches a tool (into the placeholder via the progress redirect) or posts a prose reply + promote button. Records a `kind:"command"` marker turn if the handler didn't record its own (project-stage branch doesn't), so the transcript stays continuous. Telemetry per turn: `tools_called`, `tools_ignored`, `iterations`, `replied_without_tool`.
- `chat-turn.js` — gutted to session-resolution + `agent.runTurn()`.
- `intent.js` — `detectRegexIntent`, `REGEX_INTENT`, `QUESTION_LEAD`/`isInterrogative`/`shouldRouteToCommand`/`isExplicitRunRequest`, `splitReplyTrailer`/`PROMPT_TRAILER_INSTRUCTION` all removed. Kept: `parseMention`, `validateSuggestion` (+ `REASON_MESSAGE`), `composeIdeaInput`, `isAnaphoric`.
- Offer mechanism removed: `promote-button.js` down to the promote button only; `index.js` `run_suggested` / `RUN_ACTION_ID` / `OFFER_TTL_MS` deleted; `eval-event.js` D-51/D-52 fields → agent fields.
- **Session state stays exportable** (the hard requirement): only `{role, text, ...}` turns are persisted — no `tool_calls`/`tool_call_id`/`function` envelopes ever hit `chat-session.js`'s disk mirror. Verified in `_agent.js`.
- `@Mill`, slash commands, buttons unchanged — deliberate invocations, straight to `command-shim`, gates intact (`_d51gates.js` still 6/6).

**Phase 2 — `/proto` inner build loop.** `runProto` now, for an executable artifact: build → run in the Part 10 sandbox → on non-zero exit, feed the file + stderr (+ timeout/error) back to the model for a full-file fix → re-run. Capped at `MILL_PROTO_BUILD_ITERS` (default 3); stops early if the same stderr repeats (no progress). Non-executable artifacts (`.html`/`.md`) skip the loop. `proto/<n>/build-log.md` records every attempt. `scratchRunner` is injectable for tests. Build iterations ≠ touches (touch cap still 5, separate counter). Entirely inside `run.sh` — D-06/D-48 unchanged. Telemetry: `build_iterations`, `build_succeeded`. `_protoloop.js`: converges / caps at 3 / no-progress stop / non-executable skip, all green. ~$0.014 for a 3-iteration build.

**dsh investigation** (`ops/dsh-investigation.md`) — time-boxed, metadata only. Confirmed: `@deepseek-ai/dsh@0.1.1-rc.2`, MIT, Cordis-plugin architecture, ships `dsh-headless` (embed bundle, no Host/HTTP/browser). Both load-bearing questions unanswerable from metadata: (1) embed footprint on 2 GB — designed path exists but ~40 sub-packages + native addons, unmeasured; (2) sandbox seam — `dsh-tool-bash` has "sandbox-escalation support" but whether our `run.sh` is a drop-in function or dsh owns the container (→ full D-06/D-48 re-audit) needs the `.d.ts`. Recommendation: **not now** (pre-1.0, the exact D-03 concern), strong "revisit at 1.0" with a 5-step spike. Founders' decision; reopens D-43.

Tests: `_agent.js` 14/14, `_protoloop.js` 15/15, `_bug12.js` 12/12, `_statecard.js` 24/24, `_d51gates.js` 6/6. Obsolete scaffolds removed (`_rcab.js`, `_interrog.js`, `_d51test.js`). Conformance 26/26 (C-06 audit-context path unchanged — tools call the same handlers). Bot restarted clean.

Docs: **D-53** in `DECISIONS.md` (amends D-43's premise, keeps its conclusion); `COMMANDS.md` / `PROJECTS.md` invocation sections rewritten to two paths; `EVAL.md` metric + telemetry-field swap; `ops/dsh-investigation.md`.

---

## D-53 follow-up — mill-flash budget + two research modes

**`mill-flash` budget $0.50 → $1.00/day** via LiteLLM `/key/update` (key value + spend preserved; models still `["flash-fast"]`, `budget_duration` still `1d`). D-53 put a `flash-fast` agent-loop call on *every* conversational turn — previously only slash/brainstorm hit `flash-fast` — and today's spend had already reached $0.502 and was tripping the cap. `docs/build-guide.md` Part 7.3 and D-23 updated in the same commit (live-config convention).

**Mode 1 — inline surface search, agent-initiated.** `search.js` `inlineFactSearch(query)` — 1–2 Tavily queries (`maxQueries:2, maxResults:3`), returns a ~1600-char findings string. `tools.js`: `find` schema gains `mode: "quick" | "broad"`; `mode:"quick"` → `inlineFactSearch`, `{posted:false, search:"agent", result}` → the agent loop continues, the model folds the fact into its prose reply. `agent.js` appends the exact marker `_(quick web check — not verified, not evidence)_` if the reply doesn't already carry a not-evidence phrase. **Trigger wording is in `agent.js`'s "WEB FACTS" block** — narrow by design: "search when you need a concrete fact to answer well, not when you feel unsure"; "I'm not certain" is not a trigger; if searching on >~1/5 of turns you're fact-checking uncertainty, stop.

**Mode 2 — broad, founder-pushed.** `tavily.js` `search(queries, {maxQueries, maxResults})` and `searchOne(query, {maxResults})` parameterized. `commands/find.js` `runFind(topic, {broad})`: broad → `planQueries(max:5)` + `search({maxQueries:5, maxResults:8})`, header `*🔎 Surface search (broad):*`. `command.broad` threaded via `dispatchCommand`. Every founder invocation of `find` (slash, `@Mill`, or the agent's broad mode because the founder asked) → `broad:true`, `search_initiated_by:"founder"`. Still `search_depth:"basic"` — breadth, not a research pass.

**Telemetry.** `search_initiated_by: "agent" | "founder" | null` on `chat`/`project_turn` and `find` events (`eval-event.js`). `EVAL.md` Layer 2: agent-initiated search rate metric, target <~1/5 of turns — a loose trigger shows in telemetry rather than inferred.

**C-23 hardened.** The agent's broad-search turn emits two same-model events for one set of LiteLLM rows (the agent call + the `find` handler's resolve/plan/summarise calls). C-23 now sums co-timed same-model sampled events and compares that sum to the spend-row sum, so a correctly-split pair isn't double-flagged. (Third time this co-timed-event shape has hit C-23; fixed properly rather than bumping the cutoff again.)

Tests: `_agent.js` now 24/24 incl. founder-broad (posted block, `search_initiated_by:founder`), agent-inline ("is India's GST threshold ₹20 or ₹40 lakh" → folded "₹20 lakh for service exporters…" + marker, no block, `search_initiated_by:agent`), and the guard (uncertainty/abstraction "i'm not sure this holds together" → no search). `_protoloop.js` 15/15, `_bug12.js` 12/12, `_d51gates.js` 6/6. Conformance 26/26. Bot restarted clean.

Docs: D-53 gains the research-modes section; `COMMANDS.md`/`PROJECTS.md` `/find` spec; `EVAL.md` metric + field; `build-guide.md`/D-23 for the budget.

---

## Fix — broad `/find` `msg_too_long` hung the "Thinking…" placeholder (2026-08-30)

Live: a founder's broad agent-`find` produced a summary that tripped Slack's `msg_too_long`; `commands/find.js`'s own catch swallowed it and posted "`/find` failed" to *channel root*, leaving the threaded `_Thinking…_` placeholder hung forever. (An earlier turn in the same thread had also failed with HTTP 429 = the `mill-flash` $0.50/day cap, since raised to $1.00.)

- **`commands/find.js` `runFind`:** broad dialled back 5×8 → **4×6** (still breadth, not a research pass); Tavily `content` per result capped at 600 chars, corpus capped at 24k; summary `maxTokens` 2048 → 1200; `topic` normalised + capped at 300 chars (`shortTopic`) before planning; **body assembled within a ~2650-char budget with the not-evidence footer always appended last** so it renders whole in one Block Kit section and can't exceed Slack limits. `_findlen.js` verifies (body ≤ 2900, footer kept).
- **`reply.js` `withProgress`:** tracks `progressState.consumed` — set true only when the placeholder is actually written (via `postResult`'s redirect *or* a direct `chat.update` to the placeholder ts, so `/test`/`/proto` stage lines count). A thrown `chat.update` leaves it false.
- **`command-shim.js` `dispatchCommand`:** returns `{ ok, progressConsumed }`.
- **`tools.js` `runTool`:** if a tool's handler ran but `progressConsumed === false` (its own catch swallowed a Slack error), returns `posted: false` with a "couldn't post its output, likely too long — suggest a narrower query" result, so the agent loop composes a real reply into the placeholder instead of hanging.
- **`commands/find.js` catch:** posts the failure via `postResult` with `thread_ts` (lands in the placeholder / thread), not `client.chat.postMessage` to channel root.
- Cleared the live hung placeholder by hand (`chat.update`).

Also, surfaced by conformance during the fix (both pre-existing, not from this change):
- **C-17 was failing** — `mill-build` Docker network gone (pruned; the weekly Docker cleanup removes unused networks and `mill-build` has no persistent container). `systemctl restart mill-sandbox-net` recreated it + the DOCKER-USER rules. **The weekly cleanup pruning the sandbox networks is a real bug — needs an exclude or a re-create-after-prune step.**
- **C-23** — improved the co-timed-event matcher to sum *all* post-fix same-model telemetry events in a window (not just sampled ones) vs the spend-row sum. A remaining tiny gap: a `find` that fails after making calls logs `status:"failed"` with no `cost_usd` (~$0.006 unlogged). `PRICING_FIX_DEPLOYED_AT` bumped to exclude the pre-fix window.

Tests: `_findlen.js` 6/6, `_agent.js` 24/24, `_protoloop.js` 15/15, `_bug12.js` 12/12, `_d51gates.js` 6/6. Conformance 26/26. Bot restarted.

---

## Revision — broad `/find` writes a stored report instead of truncating (2026-08-30)

Reverting the 2026-08-30 dial-back (that shrank broad breadth to avoid `msg_too_long`). Breadth restored to **5×8**; the length problem is solved by not putting the full result in a Slack message.

- **`commands/find.js`:** broad `runFind` returns raw pieces (`summary`, `queries`, `sources`, `shortTopic`). `writeFindReport(id, …)` writes the full report to `ideas/<id>/find/<YYYYMMDD-HHMMSS>.md` — a `> NOT evidence` header, run-by/when/queries, the full summary, every scanned source as a link — and appends a one-line entry to `ideas/<id>/find/index.md`. `handleFindCommand`: in a project (`findIdeaByChannel(channel_id)`) → write report + `commitAndPush([ideas/<id>/find])` + post a `findRundown` (capped ~2700, keeps the not-evidence footer, names the saved path) + `uploadThreadFile` the `.md` to the thread. In `#chats` (no project) → unchanged capped inline message.
- **`slack-files.js`** (new): `uploadThreadFile` via `files.uploadV2`. Best-effort — **needs the `files:write` scope, which the bot token does not have** (current scopes end at `pins:write`). Without it: `missing_scope`, logged once, and the report is still committed + named in the rundown. **Add `files:write` to enable attachments.**
- **`ideas.js`:** `readFindIndex(id)`, `readFindReport(id, stamp?)`.
- **`chat-session.js` `readOriginContext`:** appends the `find/index.md` block, labelled "NOT evidence — never cite as such", so every project stage thread can reference a report run in any other thread (mechanism reused from D-52 Root Cause B's origin-context loading).
- `find/` is a distinct directory from `docs/` on purpose — `/test` passes `docs/` as GPT Researcher's `DOC_PATH` and the audit reads the `docs/` index; a surface search must never leak in as evidence (PROJECTS.md 14.5, D-33).

Tests: `_findreport.js` (13/13) — report file with NOT-evidence header + Summary/Sources, `find/index.md` entry, capped rundown naming the path, `files.uploadV2` to the thread, `readOriginContext` lists it, a second `/find` appends (2 files / 2 index lines), `#chats` stays inline with no file. `_findlen.js` 6/6, `_agent.js` 24/24, `_protoloop.js` 15/15, `_bug12.js` 12/12, `_d51gates.js` 6/6, `_statecard.js` 24/24. Conformance 26/26. Bot restarted.

Docs: D-53 research-modes section, `COMMANDS.md`/`PROJECTS.md` `/find`.
